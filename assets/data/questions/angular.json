{
  "topicId": "angular",
  "topicName": "Angular & Frontend",
  "subtopics": [
    {
      "subtopicId": "basics",
      "subtopicName": "Components & Basics",
      "order": 1,
      "questions": [
        {
          "id": "angular-basics-001",
          "q": "What is a Component in Angular?",
          "level": "junior",
          "type": "conceptual",
          "freq": "high",
          "tags": ["component", "decorator", "template", "basics"],
          "answer": {
            "oneLiner": "A Component is the basic UI building block — a TypeScript class with @Component that bundles a template, styles, and logic into a reusable unit.",
            "keyPoints": [
              "Decorated with `@Component({ selector, template, styles })`",
              "Selector lets you use it as a custom HTML element",
              "Can be standalone (v14+) or part of an NgModule",
              "Components form a tree rooted at AppComponent"
            ],
            "code": {
              "lang": "typescript",
              "snippet": "import { Component, Input } from '@angular/core';\n\n@Component({\n  selector: 'app-greeting',\n  standalone: true,\n  template: `<h1>Hello, {{ name }}!</h1>`\n})\nexport class GreetingComponent {\n  @Input() name = 'World';\n}",
              "note": "Minimal standalone component with an @Input binding"
            },
            "analogy": "Like a LEGO brick — shape (template), colour (styles), connection points (@Input/@Output).",
            "followUps": [
              "Standalone component vs one declared in an NgModule?",
              "How does Angular pick the component for a route?",
              "How would you share state between siblings?"
            ],
            "redFlags": [
              "Confusing a component with a directive",
              "Not knowing what the selector is for",
              "Assuming it works exactly like a React function component"
            ]
          }
        },
        {
          "id": "angular-basics-002",
          "q": "What are Directives and what is the difference between structural and attribute directives?",
          "level": "junior",
          "type": "conceptual",
          "freq": "high",
          "tags": ["directives", "structural", "attribute", "ngIf", "ngFor", "ngClass"],
          "answer": {
            "oneLiner": "Directives add behaviour to elements. Structural directives change the DOM layout (add/remove nodes); attribute directives change the look or behaviour of an existing element.",
            "keyPoints": [
              "Structural: `*ngIf`, `*ngFor` — the `*` is sugar for `<ng-template>`",
              "Attribute: `ngClass`, `ngStyle`, custom directives — no DOM add/remove",
              "Components are technically a directive with a template",
              "Angular 17+ adds `@if` / `@for` / `@switch` control flow blocks"
            ],
            "detail": "Only one structural directive per element — use `<ng-container>` to apply several. The new control flow blocks are the preferred, boilerplate-free alternative.",
            "code": {
              "lang": "html",
              "snippet": "<!-- Structural (old syntax) -->\n<div *ngIf=\"isLoggedIn; else guest\">Welcome back!</div>\n<ng-template #guest><p>Please log in.</p></ng-template>\n\n<!-- Structural (Angular 17+ new syntax) -->\n@if (isLoggedIn) {\n  <div>Welcome back!</div>\n} @else {\n  <p>Please log in.</p>\n}\n\n<!-- Attribute directive -->\n<p [ngClass]=\"{ 'active': isActive, 'disabled': !isActive }\">Status</p>",
              "note": "Old *ngIf vs Angular 17 @if control flow, plus ngClass attribute directive"
            },
            "followUps": [
              "How would you build a custom structural directive?",
              "What is `ng-container` and why is it useful?",
              "Difference between `ngClass` and `[class.active]`?"
            ],
            "redFlags": [
              "Saying *ngIf only hides elements (it removes from DOM)",
              "Not knowing `*` expands to `<ng-template>`",
              "Unaware of Angular 17 `@if` / `@for`"
            ]
          }
        },
        {
          "id": "angular-basics-003",
          "q": "What does the Angular compiler do? Explain AOT vs JIT and the role of Ivy.",
          "level": "mid",
          "type": "conceptual",
          "freq": "high",
          "tags": ["compiler", "AOT", "JIT", "Ivy", "build", "performance"],
          "answer": {
            "oneLiner": "The compiler turns templates and decorators into optimised JS. AOT compiles at build time (the production default), JIT compiles in the browser, and Ivy is the current compile/render engine.",
            "keyPoints": [
              "JIT: compiler shipped to browser, compiles at runtime — dev only",
              "AOT: compiled at build, smaller bundles, catches template errors early",
              "Ivy is the engine (default since v9), replacing View Engine",
              "Ivy enables tree-shaking, locality, standalone, and signals"
            ],
            "detail": "AOT is the default for both `ng build` and `ng serve` since v12. Ivy compiles each component independently (locality), which speeds up incremental builds and improves tree-shaking.",
            "code": {
              "lang": "typescript",
              "snippet": "// tsconfig.json — AOT is on by default in Angular CLI\n// angular.json build options:\n// \"aot\": true  (default for production)\n// \"buildOptimizer\": true\n\n// Ivy locality: each component compiles to a self-contained factory\n// MyComponent.ɵcmp = /*@__PURE__*/ defineComponent({ ... })",
              "note": "AOT is on by default; Ivy factories are what the Angular runtime executes"
            },
            "followUps": [
              "Benefits of `strictTemplates`?",
              "How does Ivy locality help large monorepo builds?",
              "Is differential loading still relevant in Angular 17+?"
            ],
            "redFlags": [
              "Thinking JIT is used in production",
              "Confusing Ivy with a specific Angular version",
              "Not knowing AOT catches template errors at build"
            ]
          }
        },
        {
          "id": "angular-basics-004",
          "q": "Describe Angular's component lifecycle hooks and when each fires.",
          "level": "mid",
          "type": "conceptual",
          "freq": "high",
          "tags": ["lifecycle", "ngOnInit", "ngOnChanges", "ngOnDestroy", "hooks"],
          "answer": {
            "oneLiner": "Components run through hooks from creation to destruction — the key ones are ngOnChanges, ngOnInit, ngAfterViewInit, and ngOnDestroy.",
            "keyPoints": [
              "`ngOnChanges` — on every @Input change, before ngOnInit",
              "`ngOnInit` — once after first inputs set; fetch initial data here",
              "`ngAfterViewInit` — view ready; safe to access @ViewChild",
              "`ngOnDestroy` — cleanup; unsubscribe and clear timers"
            ],
            "detail": "Order: constructor, ngOnChanges, ngOnInit, ngDoCheck, ngAfterContentInit/Checked, ngAfterViewInit/Checked, ngOnDestroy. @ViewChild is undefined in ngOnInit. With signals you can use `effect()` instead of ngDoCheck.",
            "code": {
              "lang": "typescript",
              "snippet": "import { Component, Input, OnInit, OnChanges, OnDestroy, SimpleChanges } from '@angular/core';\nimport { Subject } from 'rxjs';\nimport { takeUntil } from 'rxjs/operators';\n\n@Component({ selector: 'app-demo', standalone: true, template: '' })\nexport class DemoComponent implements OnInit, OnChanges, OnDestroy {\n  @Input() userId!: string;\n  private destroy$ = new Subject<void>();\n\n  ngOnChanges(changes: SimpleChanges) {\n    if (changes['userId'] && !changes['userId'].firstChange) {\n      // React to userId change after initial load\n    }\n  }\n\n  ngOnInit() {\n    this.dataService.getUser(this.userId)\n      .pipe(takeUntil(this.destroy$))\n      .subscribe(user => this.user = user);\n  }\n\n  ngOnDestroy() {\n    this.destroy$.next();\n    this.destroy$.complete();\n  }\n}",
              "note": "Pattern: takeUntil + destroy$ Subject to auto-unsubscribe on component destruction"
            },
            "followUps": [
              "Difference between ngOnInit and the constructor?",
              "Why can't you access @ViewChild in ngOnInit?",
              "What does `takeUntilDestroyed()` do?"
            ],
            "redFlags": [
              "Putting HTTP calls in the constructor",
              "Accessing @ViewChild in ngOnInit",
              "Not cleaning up subscriptions in ngOnDestroy"
            ]
          }
        },
        {
          "id": "angular-basics-005",
          "q": "What type of DOM does Angular use, and how does it compare to React's virtual DOM?",
          "level": "mid",
          "type": "comparison",
          "freq": "medium",
          "tags": ["DOM", "virtual DOM", "incremental DOM", "Ivy", "React", "rendering"],
          "answer": {
            "oneLiner": "Angular uses the real DOM with an incremental DOM strategy via Ivy; React uses a virtual DOM that it diffs and patches. Angular's approach is more memory efficient since there's no shadow tree.",
            "keyPoints": [
              "Angular works on the real DOM — no virtual DOM",
              "Ivy compiles templates to instructions that update only changed nodes",
              "React diffs an in-memory VDOM tree, then patches the real DOM",
              "Incremental DOM uses less memory and tree-shakes better"
            ],
            "detail": "Ivy's instructions live alongside the component, so no intermediate tree is built. Change detection (Zone.js or signals) decides when to run; Ivy instructions decide how. Both perform well in practice.",
            "analogy": "VDOM is redrawing the whole blueprint then comparing; incremental DOM is a checklist that only touches the listed items.",
            "followUps": [
              "How does OnPush reduce DOM work?",
              "How do signals eliminate Zone.js overhead?",
              "What is hydration in Angular SSR?"
            ],
            "redFlags": [
              "Claiming Angular uses a virtual DOM",
              "Not knowing what Ivy is",
              "Saying one approach is always faster"
            ]
          }
        },
        {
          "id": "angular-basics-006",
          "q": "What architecture does Angular follow?",
          "level": "junior",
          "type": "conceptual",
          "freq": "high",
          "tags": ["architecture", "MVVM", "component-based", "NgModule", "standalone", "DI"],
          "answer": {
            "oneLiner": "A component-based architecture with built-in dependency injection, loosely MVVM — components are the ViewModel binding templates (View) to services (Model).",
            "keyPoints": [
              "Component tree rooted at AppComponent",
              "Hierarchical DI for services",
              "MVVM-ish: component = ViewModel, template = View, services = Model",
              "Full opinionated framework: routing, forms, HTTP built in"
            ],
            "detail": "NgModules grouped components historically but are being superseded by standalone components (default v17+). A service in `root` is a singleton; provided in a component it's scoped to that subtree.",
            "followUps": [
              "How does Angular DI differ from manual instantiation?",
              "Advantages of standalone over NgModules?",
              "How would you structure a large Angular app?"
            ],
            "redFlags": [
              "Calling Angular a library",
              "Not knowing what DI is in Angular",
              "Confusing it with React's data flow model"
            ]
          }
        },
        {
          "id": "angular-basics-007",
          "q": "What is lazy loading in Angular and what are its advantages?",
          "level": "mid",
          "type": "conceptual",
          "freq": "high",
          "tags": ["lazy loading", "routing", "performance", "code splitting", "bundle"],
          "answer": {
            "oneLiner": "Lazy loading defers downloading a route's code until the user navigates to it, shrinking the initial bundle and speeding up startup.",
            "keyPoints": [
              "Use `loadComponent` (standalone) or `loadChildren` in routes",
              "CLI auto code-splits each lazy route into its own chunk",
              "Reduces initial bundle and improves Time to Interactive",
              "Combine with preloading strategies for background loading"
            ],
            "detail": "Lazy routes get their own injector scope, so services provided there don't leak. Preloading options: `NoPreloading` (default), `PreloadAllModules`, or a custom strategy.",
            "code": {
              "lang": "typescript",
              "snippet": "// app.routes.ts — standalone lazy loading\nimport { Routes } from '@angular/router';\n\nexport const APP_ROUTES: Routes = [\n  { path: '', loadComponent: () =>\n      import('./home/home.component').then(m => m.HomeComponent) },\n  { path: 'dashboard', loadChildren: () =>\n      import('./dashboard/dashboard.routes').then(m => m.DASHBOARD_ROUTES) }\n];",
              "note": "loadComponent for a single component, loadChildren for a sub-route array"
            },
            "followUps": [
              "How do you implement a custom preloading strategy?",
              "loadChildren with a module vs a route array?",
              "How does lazy loading interact with DI scoping?"
            ],
            "redFlags": [
              "Thinking lazy loading hides routes from users",
              "Not knowing `loadComponent` is the modern way",
              "Confusing it with ngIf show/hide"
            ]
          }
        },
        {
          "id": "angular-basics-008",
          "q": "What are standalone components and how do they differ from NgModule-based components?",
          "level": "mid",
          "type": "comparison",
          "freq": "high",
          "tags": ["standalone", "NgModule", "Angular 14", "Angular 17", "imports"],
          "answer": {
            "oneLiner": "Standalone components declare their own imports directly in @Component instead of belonging to an NgModule, removing the NgModule boilerplate layer.",
            "keyPoints": [
              "`standalone: true` (default in Angular 17+)",
              "Component lists its own `imports: [...]`",
              "No @NgModule declaration needed",
              "Bootstrapped with `bootstrapApplication(AppComponent, ...)`"
            ],
            "detail": "Standalone components can still import legacy NgModules like `ReactiveFormsModule`. Migrate with `ng generate @angular/core:standalone`.",
            "code": {
              "lang": "typescript",
              "snippet": "// main.ts — standalone bootstrap\nimport { bootstrapApplication } from '@angular/platform-browser';\nimport { AppComponent } from './app/app.component';\nimport { provideRouter } from '@angular/router';\nimport { APP_ROUTES } from './app/app.routes';\n\nbootstrapApplication(AppComponent, {\n  providers: [provideRouter(APP_ROUTES)]\n});",
              "note": "Standalone apps use bootstrapApplication instead of platformBrowserDynamic().bootstrapModule()"
            },
            "followUps": [
              "How do you provide a service to just a standalone subtree?",
              "Can a standalone component use an NgModule directive?",
              "Why use `provideRouter` instead of `RouterModule.forRoot`?"
            ],
            "redFlags": [
              "Not knowing standalone is the default in v17",
              "Thinking standalone can't use NgModule libraries",
              "Confusing standalone with micro-frontends"
            ]
          }
        },
        {
          "id": "angular-basics-009",
          "q": "What is an Angular Service and how does Dependency Injection work?",
          "level": "junior",
          "type": "conceptual",
          "freq": "high",
          "tags": ["service", "dependency injection", "DI", "providedIn", "singleton"],
          "answer": {
            "oneLiner": "A Service is a reusable @Injectable class for logic, HTTP, or shared state. Angular's DI injects it via the constructor or the inject() function.",
            "keyPoints": [
              "`@Injectable({ providedIn: 'root' })` = app-wide singleton",
              "Inject via constructor or `inject()`",
              "Hierarchical injector — provide in a component to scope it",
              "DI enables testability via mock injection"
            ],
            "detail": "Injector hierarchy: platform > root > environment (lazy scopes) > component. Provide in `@Component.providers` to scope a service to that component tree.",
            "code": {
              "lang": "typescript",
              "snippet": "@Injectable({ providedIn: 'root' })\nexport class TodoService {\n  private http = inject(HttpClient); // modern inject() function\n  \n  getTodos() {\n    return this.http.get<Todo[]>('/api/todos');\n  }\n}\n\n@Component({ ... })\nexport class TodoListComponent {\n  private todoSvc = inject(TodoService); // inject() preferred in standalone\n  todos$ = this.todoSvc.getTodos();\n}",
              "note": "Modern Angular uses inject() function instead of constructor injection — works outside of constructors"
            },
            "followUps": [
              "inject() function vs constructor injection?",
              "How to provide different impls per environment?",
              "What is a multi-provider token?"
            ],
            "redFlags": [
              "Not knowing what `providedIn: 'root'` means",
              "Creating services with `new` instead of injecting",
              "Not seeing how DI enables testing"
            ]
          }
        },
        {
          "id": "angular-basics-010",
          "q": "What is the Angular CLI and what are its most important commands?",
          "level": "junior",
          "type": "conceptual",
          "freq": "medium",
          "tags": ["CLI", "ng", "generate", "build", "serve", "test"],
          "answer": {
            "oneLiner": "The CLI scaffolds, builds, serves, tests, and lints Angular apps with no manual build config.",
            "keyPoints": [
              "`ng new` — scaffold a project; `ng serve` — dev server",
              "`ng generate` (`ng g c`) — scaffold components/services/etc.",
              "`ng build --configuration=production` — AOT, minified bundle",
              "`ng test`, `ng e2e`, `ng update` — test and safe migrations"
            ],
            "detail": "The CLI abstracts webpack and uses esbuild by default since v17 (the `application` builder) for much faster builds. `ng add` runs a package's schematic to install and configure it.",
            "followUps": [
              "How to configure environment-specific builds?",
              "`ng build` vs `ng build --configuration production`?",
              "What is the v17 `application` builder?"
            ],
            "redFlags": [
              "Not knowing the basic generate commands",
              "Editing webpack config directly",
              "Confusing `ng update` with `npm update`"
            ]
          }
        },
        {
          "id": "angular-basics-011",
          "q": "What are smart (container) and dumb (presentational) components?",
          "level": "mid",
          "type": "design",
          "freq": "medium",
          "tags": ["smart components", "dumb components", "presentational", "container", "design pattern"],
          "answer": {
            "oneLiner": "Smart (container) components manage state and services; dumb (presentational) components just take @Input and emit @Output with no service dependencies.",
            "keyPoints": [
              "Smart: inject services, orchestrate children",
              "Dumb: pure UI driven by inputs/outputs",
              "Dumb components pair well with OnPush",
              "Improves testability and reusability"
            ],
            "detail": "It's a guideline, not a rule — some components legitimately need both. Maps to flux/redux: smart = connected, dumb = presentational.",
            "followUps": [
              "When is it OK to inject a service in a dumb component?",
              "How does this interact with NgRx or a signal store?",
              "How does OnPush benefit dumb components?"
            ],
            "redFlags": [
              "Not knowing the pattern for a senior role",
              "Thinking 'dumb' means bad",
              "Injecting services into presentational components"
            ]
          }
        },
        {
          "id": "angular-basics-012",
          "q": "What are ViewChild and ContentChild decorators?",
          "level": "mid",
          "type": "conceptual",
          "freq": "medium",
          "tags": ["ViewChild", "ContentChild", "template reference", "ng-content", "ElementRef"],
          "answer": {
            "oneLiner": "@ViewChild queries the component's own template; @ContentChild queries projected content (ng-content). Each is ready only after its AfterInit hook.",
            "keyPoints": [
              "@ViewChild — first match in own template",
              "@ContentChild — content projected via `<ng-content>`",
              "Ready in ngAfterViewInit / ngAfterContentInit (not ngOnInit)",
              "@ViewChildren / @ContentChildren return a QueryList"
            ],
            "detail": "`{ static: true }` makes a query available in ngOnInit but only for elements always in the DOM. Signal-based `viewChild()` / `contentChild()` (v17.1+) give reactive access without lifecycle coupling.",
            "code": {
              "lang": "typescript",
              "snippet": "// Signal-based alternative (Angular 17.1+)\nimport { viewChild, contentChild } from '@angular/core';\n\nexport class MyComponent {\n  canvas = viewChild<ElementRef>('myCanvas');\n  // canvas() returns ElementRef | undefined — reactive, no lifecycle hook needed\n}",
              "note": "viewChild() signal function — reactive alternative to @ViewChild decorator"
            },
            "followUps": [
              "@ViewChild vs @ViewChildren?",
              "Why is @ViewChild undefined in ngOnInit?",
              "How do signal-based viewChild()/contentChild() improve things?"
            ],
            "redFlags": [
              "Accessing @ViewChild in the constructor/ngOnInit without static",
              "Confusing ViewChild with ContentChild",
              "Not knowing ViewChildren returns a QueryList"
            ]
          }
        }
      ]
    },
    {
      "subtopicId": "binding",
      "subtopicName": "Data Binding & Reactivity",
      "order": 2,
      "questions": [
        {
          "id": "angular-binding-001",
          "q": "What are the four types of data binding in Angular?",
          "level": "junior",
          "type": "conceptual",
          "freq": "high",
          "tags": ["data binding", "interpolation", "property binding", "event binding", "two-way binding", "ngModel"],
          "answer": {
            "oneLiner": "Interpolation {{ }}, property binding [prop], event binding (event), and two-way binding [(ngModel)] which is just property + event combined.",
            "keyPoints": [
              "Interpolation `{{ value }}` — value as text",
              "Property `[prop]=\"expr\"` — sets a DOM property or @Input",
              "Event `(event)=\"handler($event)\"` — DOM events or @Output",
              "Two-way `[(ngModel)]` — needs FormsModule"
            ],
            "detail": "Property binding targets DOM properties, not HTML attributes — use `[attr.x]` for attributes. Banana-in-a-box `[()]` = input down + output up; pair `@Input() x` with `@Output() xChange` for custom two-way.",
            "code": {
              "lang": "html",
              "snippet": "<!-- All four binding types -->\n<h1>{{ title }}</h1>                         <!-- interpolation -->\n<input [value]=\"username\" />                 <!-- property binding -->\n<button (click)=\"submit()\">Go</button>       <!-- event binding -->\n<input [(ngModel)]=\"username\" />             <!-- two-way binding -->",
              "note": "Each binding type and its syntax at a glance"
            },
            "followUps": [
              "Difference between `[src]` and `src={{ }}`?",
              "How to bind an HTML attribute vs a DOM property?",
              "How does two-way binding work without ngModel?"
            ],
            "redFlags": [
              "Confusing HTML attributes with DOM properties",
              "Using `[(ngModel)]` in reactive forms",
              "Forgetting to import FormsModule"
            ]
          }
        },
        {
          "id": "angular-binding-002",
          "q": "What is the difference between a Promise and an Observable?",
          "level": "mid",
          "type": "comparison",
          "freq": "high",
          "tags": ["Promise", "Observable", "RxJS", "async", "reactive"],
          "answer": {
            "oneLiner": "A Promise is eager, resolves once, and can't be cancelled. An Observable is lazy, cancellable, can emit many values, and composes via RxJS operators.",
            "keyPoints": [
              "Promise: eager, single value, not cancellable",
              "Observable: lazy, 0–N values, cancellable via unsubscribe",
              "Observables have operators (map, switchMap, etc.)",
              "Angular HTTP returns Observables; async pipe auto-manages them"
            ],
            "detail": "Bridge with `firstValueFrom()`/`lastValueFrom()` (Observable to Promise) and `from()` (Promise to Observable). Prefer Observables for HTTP, streams, and complex async pipelines.",
            "followUps": [
              "Why does HttpClient return an Observable?",
              "How do you convert an Observable to a Promise?",
              "`firstValueFrom` vs deprecated `toPromise`?"
            ],
            "redFlags": [
              "Saying Observables are just fancier Promises",
              "Not knowing Observables are lazy",
              "Not knowing how to cancel an HTTP request"
            ]
          }
        },
        {
          "id": "angular-binding-003",
          "q": "What are @Input and @Output and how do they enable component communication?",
          "level": "junior",
          "type": "conceptual",
          "freq": "high",
          "tags": ["@Input", "@Output", "EventEmitter", "component communication", "parent-child"],
          "answer": {
            "oneLiner": "@Input passes data down from parent to child; @Output emits events up from child to parent via EventEmitter.",
            "keyPoints": [
              "`@Input()` — parent binds `[prop]=\"value\"` (data down)",
              "`@Output()` EventEmitter — parent listens `(event)=\"...\"` (events up)",
              "Inputs support `required` and `transform` (v16+)",
              "Signal `input()` / `output()` (v17.1+) are reactive alternatives"
            ],
            "detail": "For non parent-child communication, use a shared service or state store. Signal inputs are readable in computed/effect without ngOnChanges.",
            "code": {
              "lang": "typescript",
              "snippet": "// Modern signal-based (Angular 17.1+)\nimport { input, output } from '@angular/core';\n\nexport class ProductCardComponent {\n  product = input.required<Product>(); // required input signal\n  addToCart = output<Product>();        // output signal\n\n  onAdd() {\n    this.addToCart.emit(this.product()); // call signal to read value\n  }\n}",
              "note": "Signal inputs are reactive — usable in computed() and effect() without ngOnChanges"
            },
            "followUps": [
              "How do you alias an @Input?",
              "How do signal inputs affect change detection?",
              "How to pass data between sibling components?"
            ],
            "redFlags": [
              "Mutating an @Input directly in the child",
              "Not knowing @Output uses EventEmitter",
              "Not knowing signal input/output for a senior role"
            ]
          }
        },
        {
          "id": "angular-binding-004",
          "q": "What is the difference between Reactive Forms and Template-driven Forms? When should you use each?",
          "level": "mid",
          "type": "comparison",
          "freq": "high",
          "tags": ["reactive forms", "template-driven forms", "FormControl", "ngModel", "validation", "forms"],
          "answer": {
            "oneLiner": "Reactive forms define the model in TypeScript (FormGroup/FormControl) for full control and testability; template-driven forms use ngModel in the template and suit simple cases.",
            "keyPoints": [
              "Reactive: model in class, `ReactiveFormsModule`, FormGroup/Array",
              "Template-driven: model from ngModel, `FormsModule`, simpler",
              "Reactive is easier to test and supports dynamic fields",
              "Reactive forms are typed since Angular 14+"
            ],
            "detail": "Use reactive for complex, dynamic, or cross-field-validated forms; template-driven for quick simple forms. Don't mix ngModel and formControl on one element.",
            "followUps": [
              "How do you do cross-field validation in reactive forms?",
              "What is FormArray and when to use it?",
              "How do typed reactive forms help?"
            ],
            "redFlags": [
              "Mixing ngModel and formControl on one element",
              "Not knowing how to read validation errors",
              "Choosing template-driven for a dynamic wizard"
            ]
          }
        },
        {
          "id": "angular-binding-005",
          "q": "What is RxJS? Explain Observables, operators, and subscriptions.",
          "level": "mid",
          "type": "conceptual",
          "freq": "high",
          "tags": ["RxJS", "Observable", "operator", "subscription", "reactive programming"],
          "answer": {
            "oneLiner": "RxJS is a reactive library built on Observables — lazy, composable, cancellable streams — with operators like map, filter, and switchMap to transform and combine them.",
            "keyPoints": [
              "Observable: lazy stream; runs only on subscribe",
              "Subscription: the execution; unsubscribe to cancel",
              "Operators chain via `.pipe()` to transform streams",
              "Angular uses RxJS in HttpClient, Router, valueChanges"
            ],
            "detail": "A Subject is both Observable and Observer and can multicast. Prefer the `async` pipe over manual subscriptions to avoid leaks.",
            "code": {
              "lang": "typescript",
              "snippet": "searchResults$ = this.searchControl.valueChanges.pipe(\n  debounceTime(300),           // wait 300ms after last keystroke\n  distinctUntilChanged(),      // ignore if same as last value\n  filter(q => q.length > 2),   // minimum 3 chars\n  switchMap(q => this.api.search(q)), // cancel previous request\n  catchError(err => of([]))    // handle errors gracefully\n);",
              "note": "Typical type-ahead pipeline with debounce, distinct, and switchMap"
            },
            "followUps": [
              "Difference between map and switchMap?",
              "combineLatest vs forkJoin?",
              "How to handle errors without killing the stream?"
            ],
            "redFlags": [
              "Nested subscriptions instead of flattening operators",
              "Not unsubscribing from infinite Observables",
              "Misusing switchMap when concatMap is needed"
            ]
          }
        },
        {
          "id": "angular-binding-006",
          "q": "How do you prevent memory leaks from RxJS subscriptions in Angular?",
          "level": "mid",
          "type": "scenario",
          "freq": "high",
          "tags": ["memory leak", "unsubscribe", "async pipe", "takeUntilDestroyed", "takeUntil", "RxJS"],
          "answer": {
            "oneLiner": "Use the async pipe (auto-unsubscribes), takeUntilDestroyed() (v16+), or a destroy$ Subject with takeUntil.",
            "keyPoints": [
              "`async` pipe — auto subscribe/unsubscribe in templates",
              "`takeUntilDestroyed()` (v16+) — cleanest imperative way",
              "`takeUntil(destroy$)` — classic Subject + ngOnDestroy",
              "Finite Observables (take(1), HTTP) complete on their own"
            ],
            "detail": "`DestroyRef.onDestroy()` covers non-component contexts. HTTP completes after one emission, but switchMap/interval pipelines still need cleanup.",
            "code": {
              "lang": "typescript",
              "snippet": "export class MyComponent {\n  private destroyRef = inject(DestroyRef);\n\n  ngOnInit() {\n    this.dataService.stream$\n      .pipe(takeUntilDestroyed(this.destroyRef))\n      .subscribe(data => this.data = data);\n  }\n}",
              "note": "takeUntilDestroyed with an injected DestroyRef — the modern pattern"
            },
            "followUps": [
              "What is the DestroyRef token?",
              "How does toSignal() handle cleanup?",
              "Are HttpClient subscriptions safe to leave?"
            ],
            "redFlags": [
              "Not knowing subscription leaks exist",
              "Only knowing the destroy$ pattern, not takeUntilDestroyed",
              "Thinking all HttpClient subscriptions always complete"
            ]
          }
        },
        {
          "id": "angular-binding-007",
          "q": "What is the difference between Subject, BehaviorSubject, ReplaySubject, and AsyncSubject?",
          "level": "mid",
          "type": "comparison",
          "freq": "high",
          "tags": ["Subject", "BehaviorSubject", "ReplaySubject", "RxJS", "multicast"],
          "answer": {
            "oneLiner": "Subject is a basic multicast; BehaviorSubject holds a current value for late subscribers; ReplaySubject replays N past values; AsyncSubject emits only the last value on complete.",
            "keyPoints": [
              "Subject — no initial value, late subscribers miss past emissions",
              "BehaviorSubject — has current value via `.value`, needs an initial",
              "ReplaySubject(n) — replays last n to new subscribers",
              "AsyncSubject — emits only the final value on complete"
            ],
            "detail": "BehaviorSubject is the common choice for shared state in services. Expose it via `asObservable()` so consumers can't call `.next()`.",
            "code": {
              "lang": "typescript",
              "snippet": "@Injectable({ providedIn: 'root' })\nexport class CartService {\n  private items$ = new BehaviorSubject<CartItem[]>([]);\n  readonly cart$ = this.items$.asObservable(); // expose as read-only Observable\n  \n  addItem(item: CartItem) {\n    this.items$.next([...this.items$.value, item]);\n  }\n}",
              "note": "asObservable() prevents consumers from calling .next() directly"
            },
            "followUps": [
              "Why expose BehaviorSubject as asObservable()?",
              "When to choose ReplaySubject over BehaviorSubject?",
              "BehaviorSubject vs a Signal for state?"
            ],
            "redFlags": [
              "Not knowing BehaviorSubject needs an initial value",
              "Exposing the Subject itself so consumers can call .next()",
              "Confusing Subject with BehaviorSubject"
            ]
          }
        },
        {
          "id": "angular-binding-008",
          "q": "What is the difference between hot and cold Observables?",
          "level": "senior",
          "type": "conceptual",
          "freq": "medium",
          "tags": ["hot observable", "cold observable", "multicast", "RxJS", "share"],
          "answer": {
            "oneLiner": "Cold Observables create a new producer per subscriber (each subscribe re-runs the HTTP call); hot Observables share one producer among all subscribers (WebSocket, DOM events).",
            "keyPoints": [
              "Cold — independent execution per subscriber (HttpClient.get)",
              "Hot — shared stream (fromEvent, Subject, WebSocket)",
              "`share()` / `shareReplay(1)` make a cold Observable hot",
              "`shareReplay(1)` also caches the last value — good for HTTP"
            ],
            "detail": "Subscribing twice to a cold HTTP Observable fires two requests; use `shareReplay(1)` to dedupe and cache. `Router.events` is hot; `HttpClient` is cold.",
            "code": {
              "lang": "typescript",
              "snippet": "const shared$ = this.http.get('/api/data').pipe(\n  shareReplay(1) // one HTTP call, cached for late subscribers\n);\nshared$.subscribe(d => console.log('A:', d)); // triggers request\nshared$.subscribe(d => console.log('B:', d)); // gets cached value",
              "note": "shareReplay(1) dedupes the request and caches the result"
            },
            "followUps": [
              "share() vs shareReplay(1)?",
              "What happens with async pipe twice on the same cold http$?",
              "How would you cache API calls with shareReplay?"
            ],
            "redFlags": [
              "Not knowing async pipe twice = two HTTP requests",
              "Confusing multicasting with caching",
              "Not knowing what shareReplay does"
            ]
          }
        },
        {
          "id": "angular-binding-009",
          "q": "What are Angular Signals and how do they compare to RxJS for reactivity?",
          "level": "senior",
          "type": "comparison",
          "freq": "high",
          "tags": ["signals", "RxJS", "reactivity", "computed", "effect", "Angular 16"],
          "answer": {
            "oneLiner": "Signals (v16+) are synchronous, fine-grained reactive values that auto-track dependencies; RxJS is better for async streams. Use signals for state, RxJS for async flows.",
            "keyPoints": [
              "`signal(v)` — read `sig()`, write `.set()` / `.update()`",
              "`computed()` — derived, lazily recomputed",
              "`effect()` — side effects on signal change, auto-cleaned",
              "Signals enable zoneless change detection"
            ],
            "detail": "RxJS wins for async streams, time operators, and multicasting. Bridge with `toSignal(obs$)` and `toObservable(sig)`.",
            "code": {
              "lang": "typescript",
              "snippet": "export class CounterComponent {\n  count = signal(0);\n  doubled = computed(() => this.count() * 2); // auto-updates\n  \n  increment() { this.count.update(n => n + 1); }\n}",
              "note": "computed() derives reactively; read signals by calling them"
            },
            "followUps": [
              "signal.set() vs signal.update()?",
              "How does computed() avoid recomputation?",
              "What is zoneless change detection?"
            ],
            "redFlags": [
              "Thinking signals replace RxJS entirely",
              "Not knowing toSignal/toObservable",
              "Using effect() for state derivation instead of computed()"
            ]
          }
        }
      ]
    },
    {
      "subtopicId": "advanced",
      "subtopicName": "Advanced Angular",
      "order": 3,
      "questions": [
        {
          "id": "angular-advanced-001",
          "q": "How does Angular change detection work? Explain Zone.js, dirty checking, and OnPush.",
          "level": "senior",
          "type": "conceptual",
          "freq": "high",
          "tags": ["change detection", "Zone.js", "OnPush", "dirty checking", "performance"],
          "answer": {
            "oneLiner": "Zone.js intercepts async events, then Angular walks the component tree top-down checking bindings. OnPush skips a component unless its input reference changes, it emits an event, or an async pipe emits.",
            "keyPoints": [
              "Zone.js patches async APIs to trigger a CD cycle",
              "Default CD checks every component each cycle",
              "OnPush re-checks on input change, event, async emit, or markForCheck()",
              "Signals (v16+) enable fine-grained, zoneless updates"
            ],
            "detail": "OnPush needs immutable data — replace references, don't mutate. Use markForCheck() to flag a component dirty from outside the event flow (e.g. WebSocket push).",
            "code": {
              "lang": "typescript",
              "snippet": "@Component({\n  selector: 'app-list',\n  changeDetection: ChangeDetectionStrategy.OnPush,\n  template: `\n    @for (item of items(); track item.id) {\n      <li>{{ item.name }}</li>\n    }\n  `\n})\nexport class ListComponent {\n  items = input<Item[]>([]);  // signal input — fine-grained reactivity\n}",
              "note": "OnPush + signal input = minimal change detection overhead"
            },
            "followUps": [
              "What if you mutate an array @Input on an OnPush component?",
              "detectChanges() vs markForCheck()?",
              "How do signals remove the need for Zone.js?"
            ],
            "redFlags": [
              "Not knowing what Zone.js does",
              "Mutating data with OnPush and expecting updates",
              "Using detach() without detectChanges()"
            ]
          }
        },
        {
          "id": "angular-advanced-002",
          "q": "What are Angular route guards? Explain CanActivate, CanDeactivate, CanActivateChild, and Resolve.",
          "level": "mid",
          "type": "conceptual",
          "freq": "high",
          "tags": ["route guards", "CanActivate", "CanDeactivate", "Resolve", "routing", "security"],
          "answer": {
            "oneLiner": "Guards control navigation: CanActivate blocks entry, CanDeactivate prevents leaving, CanActivateChild guards children, and Resolve pre-fetches data before activation.",
            "keyPoints": [
              "CanActivate — return true, false, or a UrlTree to redirect",
              "CanDeactivate — 'unsaved changes' confirmation",
              "Resolve — pre-fetch data before the route loads",
              "CanMatch (v14+) — prevent a route matching at all"
            ],
            "detail": "Modern Angular uses functional guards (plain functions with inject()) instead of classes. Multiple guards run sequentially; any false/UrlTree cancels navigation.",
            "code": {
              "lang": "typescript",
              "snippet": "// Functional role guard\nexport const roleGuard = (requiredRole: string): CanActivateFn => {\n  return () => {\n    const auth = inject(AuthService);\n    return auth.hasRole(requiredRole) || inject(Router).createUrlTree(['/forbidden']);\n  };\n};\n\n// Usage in routes:\n{ path: 'admin', canActivate: [roleGuard('ADMIN')], ... }",
              "note": "Factory function pattern to create parameterised guards"
            },
            "followUps": [
              "CanActivate vs CanMatch?",
              "How to guard on an async condition?",
              "Resolve vs fetching in ngOnInit — perf impact?"
            ],
            "redFlags": [
              "Using class guards when functional is the modern way",
              "Not knowing a UrlTree redirects",
              "Using Resolve on every route"
            ]
          }
        },
        {
          "id": "angular-advanced-003",
          "q": "What is the difference between switchMap, mergeMap, concatMap, and exhaustMap?",
          "level": "senior",
          "type": "comparison",
          "freq": "high",
          "tags": ["switchMap", "mergeMap", "concatMap", "exhaustMap", "RxJS", "flattening"],
          "answer": {
            "oneLiner": "All map a value to an inner Observable and flatten it; they differ in concurrency: switchMap cancels the previous, concatMap queues, mergeMap runs in parallel, exhaustMap ignores new emissions while busy.",
            "keyPoints": [
              "switchMap — cancels previous; use for type-ahead search",
              "concatMap — sequential, preserves order; use for save queues",
              "mergeMap — concurrent; use when order doesn't matter",
              "exhaustMap — ignores new while busy; use for login buttons"
            ],
            "detail": "Picking the wrong one causes classic bugs — duplicate requests, stale data, or lost clicks.",
            "code": {
              "lang": "typescript",
              "snippet": "// switchMap — search\nthis.searchControl.valueChanges.pipe(\n  debounceTime(300),\n  switchMap(query => this.api.search(query))\n).subscribe(results => ...);\n\n// exhaustMap — login button\nthis.loginClick$.pipe(\n  exhaustMap(creds => this.auth.login(creds))\n).subscribe(...);\n\n// concatMap — upload queue\nfrom(fileQueue).pipe(\n  concatMap(file => this.upload(file))\n).subscribe(...);",
              "note": "Each operator matched to its idiomatic use case"
            },
            "followUps": [
              "What happens to an in-flight request when switchMap switches?",
              "When does mergeMap break where concatMap works?",
              "How does exhaustMap prevent duplicate button clicks?"
            ],
            "redFlags": [
              "Using mergeMap for type-ahead (stale, out-of-order)",
              "Using switchMap for a save queue (cancels saves)",
              "Not knowing these operators at senior level"
            ]
          }
        },
        {
          "id": "angular-advanced-004",
          "q": "What are Angular Signals and how do they work internally?",
          "level": "senior",
          "type": "conceptual",
          "freq": "high",
          "tags": ["signals", "computed", "effect", "reactivity", "Angular 16", "zoneless"],
          "answer": {
            "oneLiner": "Signals track read dependencies automatically; when a signal changes it marks only its dependent computeds and effects as dirty and updates them, no Zone.js needed.",
            "keyPoints": [
              "Reading a signal in a reactive context registers it as a dependency",
              "computed() re-runs only when a tracked signal changes",
              "effect() runs eagerly and re-runs on any read signal change",
              "Push-pull: change pushes 'dirty', consumers pull lazily"
            ],
            "detail": "computed() is lazy — recomputes on read after a dependency changed. Expose read-only via `signal.asReadonly()`. v18+ adds `linkedSignal` and the `resource()` API for async data.",
            "code": {
              "lang": "typescript",
              "snippet": "import { signal, computed, effect } from '@angular/core';\n\nconst firstName = signal('Jane');\nconst lastName = signal('Doe');\nconst fullName = computed(() => `${firstName()} ${lastName()}`);\n\neffect(() => console.log('Name is now:', fullName()));\n// Console: 'Name is now: Jane Doe'\n\nfirstName.set('John');\n// Console: 'Name is now: John Doe'  (auto re-runs)",
              "note": "computed() lazily derives; effect() eagerly runs side effects on dependency change"
            },
            "followUps": [
              "signal.set() vs signal.update()?",
              "Can you use signals outside components?",
              "What is linkedSignal?"
            ],
            "redFlags": [
              "Thinking signals are just a BehaviorSubject wrapper",
              "Using effect() to derive state instead of computed()",
              "Not knowing computed() is lazy"
            ]
          }
        },
        {
          "id": "angular-advanced-005",
          "q": "What is the trackBy function in ngFor/@for and why is it important for performance?",
          "level": "mid",
          "type": "conceptual",
          "freq": "high",
          "tags": ["trackBy", "ngFor", "@for", "performance", "DOM recycling", "change detection"],
          "answer": {
            "oneLiner": "trackBy gives each list item a stable identity so Angular reuses DOM nodes when the list changes instead of destroying and recreating everything.",
            "keyPoints": [
              "Without it, a new array reference recreates all DOM nodes",
              "trackBy returns a unique id (usually item.id)",
              "Reuses nodes for unchanged ids, only updating properties",
              "Angular 17 `@for` requires `track`"
            ],
            "detail": "Critical when a list reloads from an API — same logical items get new object references. Big win for large lists and preserves child component state.",
            "code": {
              "lang": "html",
              "snippet": "<!-- Old *ngFor syntax -->\n<li *ngFor=\"let item of items; trackBy: trackById\">{{ item.name }}</li>\n\n<!-- Angular 17+ @for syntax (track required) -->\n@for (item of items; track item.id) {\n  <li>{{ item.name }}</li>\n}",
              "note": "trackById is a class method returning item.id; @for makes track mandatory"
            },
            "followUps": [
              "What happens to child state on reload without trackBy?",
              "When is tracking by index acceptable?",
              "How does required track in @for differ from optional trackBy?"
            ],
            "redFlags": [
              "Not knowing what trackBy does",
              "Tracking by index when items can reorder",
              "Not knowing @for requires track"
            ]
          }
        },
        {
          "id": "angular-advanced-006",
          "q": "What are pure and impure pipes in Angular?",
          "level": "mid",
          "type": "conceptual",
          "freq": "medium",
          "tags": ["pure pipe", "impure pipe", "pipes", "performance", "transform"],
          "answer": {
            "oneLiner": "Pure pipes (default) only re-run when the input reference changes, so they're fast. Impure pipes run every change detection cycle — expensive but needed for mutable inputs.",
            "keyPoints": [
              "Pure — transform() only on reference change; memoised",
              "Impure (`pure: false`) — transform() every CD cycle",
              "Pure pipes must be stateless and side-effect-free",
              "The `async` pipe is impure"
            ],
            "detail": "For mutable arrays, prefer returning a new reference and keeping the pipe pure over making it impure.",
            "code": {
              "lang": "typescript",
              "snippet": "@Pipe({ name: 'currencyFormat', pure: true, standalone: true })\nexport class CurrencyFormatPipe implements PipeTransform {\n  transform(value: number, currency = 'GBP'): string {\n    return new Intl.NumberFormat('en-GB', { style: 'currency', currency }).format(value);\n  }\n}",
              "note": "Pure pipe result is cached unless value or args change by reference"
            },
            "followUps": [
              "Why is the async pipe impure?",
              "How would you make an impure filter pipe pure?",
              "Perf impact of many impure pipes?"
            ],
            "redFlags": [
              "Not knowing what the pure property does",
              "Making filter/sort pipes impure without cost awareness",
              "Not knowing async is impure"
            ]
          }
        },
        {
          "id": "angular-advanced-007",
          "q": "What are Angular HTTP Interceptors and what are common use cases?",
          "level": "mid",
          "type": "conceptual",
          "freq": "high",
          "tags": ["interceptor", "HttpClient", "middleware", "auth token", "error handling"],
          "answer": {
            "oneLiner": "Interceptors are middleware that sit on every HttpClient request and response — used for auth tokens, error handling, logging, and caching without touching each service call.",
            "keyPoints": [
              "Functional `HttpInterceptorFn` (v15+), registered via `withInterceptors([...])`",
              "Intercept outgoing requests and incoming responses via `next`",
              "Common: auth header, 401 handling, loading spinner, logging",
              "Requests are immutable — modify with `req.clone({...})`"
            ],
            "detail": "Requests pass through interceptors in order; responses come back in reverse.",
            "code": {
              "lang": "typescript",
              "snippet": "export const authInterceptor: HttpInterceptorFn = (req, next) => {\n  const token = inject(AuthService).getToken();\n  if (!token) return next(req);\n  const authReq = req.clone({\n    headers: req.headers.set('Authorization', `Bearer ${token}`)\n  });\n  return next(authReq);\n};",
              "note": "Clone the immutable request to add a header"
            },
            "followUps": [
              "How to skip an interceptor for a specific request?",
              "How to queue requests during a token refresh?",
              "Does interceptor order matter?"
            ],
            "redFlags": [
              "Not knowing requests are immutable and must be cloned",
              "Not knowing the functional interceptor syntax",
              "Adding auth headers in every service call manually"
            ]
          }
        },
        {
          "id": "angular-advanced-008",
          "q": "What are Deferrable Views (@defer) in Angular 17?",
          "level": "senior",
          "type": "conceptual",
          "freq": "medium",
          "tags": ["defer", "deferrable views", "lazy loading", "performance", "Angular 17"],
          "answer": {
            "oneLiner": "@defer is template syntax that lazily loads a component on a trigger (viewport, interaction, idle, etc.), improving initial bundle size and Core Web Vitals.",
            "keyPoints": [
              "Triggers: `on viewport`, `on idle`, `on interaction`, `on hover`, `on timer`, `when`",
              "`@placeholder` before load, `@loading` during, `@error` on failure",
              "The chunk is code-split automatically by the compiler",
              "Works at template level, not just route level"
            ],
            "detail": "`prefetch` loads the chunk in the background while showing on a different trigger, e.g. `prefetch on idle` but render `on viewport`.",
            "code": {
              "lang": "html",
              "snippet": "@defer (on interaction; prefetch on idle) {\n  <app-comment-section [postId]=\"postId\" />\n} @placeholder {\n  <button>Load comments</button>\n}",
              "note": "Shows a button; prefetches the chunk when browser is idle; renders comments on click"
            },
            "followUps": [
              "How does @defer differ from lazy routes?",
              "Can @defer depend on auth state?",
              "@placeholder vs @loading?"
            ],
            "redFlags": [
              "Not knowing @defer as a senior dev",
              "Thinking lazy loading is route-only",
              "Confusing @placeholder with @loading"
            ]
          }
        },
        {
          "id": "angular-advanced-009",
          "q": "What are Route Resolvers in Angular and when should you use them?",
          "level": "mid",
          "type": "conceptual",
          "freq": "medium",
          "tags": ["resolver", "Resolve", "routing", "data prefetch", "UX"],
          "answer": {
            "oneLiner": "A Resolver pre-fetches data before a route activates, so the data is ready in the component immediately — no in-component loading state.",
            "keyPoints": [
              "Completes before the component mounts — no spinner inside it",
              "Modern syntax: `ResolveFn<T>` with inject()",
              "Read via `ActivatedRoute.snapshot.data['key']`",
              "Best for data essential to render (e.g. edit page)"
            ],
            "detail": "Trade-off: navigation feels slower (router waits) vs a component-level skeleton. For most apps, fetching in the component with a skeleton gives better perceived UX.",
            "code": {
              "lang": "typescript",
              "snippet": "export const userResolver: ResolveFn<User> = (route) => {\n  const userService = inject(UserService);\n  const router = inject(Router);\n  return userService.getUser(route.paramMap.get('id')!).pipe(\n    catchError(() => {\n      router.navigate(['/not-found']);\n      return EMPTY; // prevents navigation if data load fails\n    })\n  );\n};",
              "note": "Return EMPTY to cancel navigation on error"
            },
            "followUps": [
              "What happens if a resolver returns EMPTY or throws?",
              "How to pass resolver data to child routes?",
              "Resolver vs ngOnInit fetch — UX trade-off?"
            ],
            "redFlags": [
              "Using resolvers for optional sidebar content",
              "Not handling resolver errors (navigation hangs)",
              "Accessing route data with the wrong key"
            ]
          }
        },
        {
          "id": "angular-advanced-010",
          "q": "How does Angular's ChangeDetectionStrategy.OnPush optimization work in practice?",
          "level": "senior",
          "type": "scenario",
          "freq": "high",
          "tags": ["OnPush", "change detection", "immutability", "performance", "ChangeDetectorRef"],
          "answer": {
            "oneLiner": "OnPush skips a component's change detection unless its @Input reference changes, it emits an event, an async pipe emits, or markForCheck() is called — a big win for large trees.",
            "keyPoints": [
              "Re-checks on: input ref change, event, async emit, markForCheck()",
              "Requires immutability — return new references, don't mutate",
              "markForCheck() for outside-the-event updates (WebSocket)",
              "Combine with signal inputs for zoneless performance"
            ],
            "detail": "`this.items = [...this.items, x]` updates; `this.items.push(x)` does not. Signals propagate changes without manual markForCheck().",
            "code": {
              "lang": "typescript",
              "snippet": "// Works with OnPush (new reference):\nthis.items = [...this.items, newItem];\n\n// Breaks OnPush (mutation, same reference):\nthis.items.push(newItem);\n\n// Mark dirty from a WebSocket push:\nthis.price = price;\nthis.cd.markForCheck();",
              "note": "Immutable updates trigger OnPush; mutations don't"
            },
            "followUps": [
              "markForCheck() vs detectChanges()?",
              "OnPush with data from a service (not @Input)?",
              "Risks of ChangeDetectorRef.detach()?"
            ],
            "redFlags": [
              "Mutating data and expecting OnPush to detect it",
              "Not knowing markForCheck vs detectChanges",
              "Not realising async pipe calls markForCheck"
            ]
          }
        },
        {
          "id": "angular-advanced-011",
          "q": "What are Angular Schematics and how does the CLI use them?",
          "level": "senior",
          "type": "conceptual",
          "freq": "low",
          "tags": ["schematics", "CLI", "ng generate", "code generation", "automation"],
          "answer": {
            "oneLiner": "Schematics are code-transformation scripts the CLI runs to scaffold files, modify code, and migrate APIs — behind ng generate, ng add, and ng update.",
            "keyPoints": [
              "Functions that take a virtual `Tree` and return a modified one",
              "`ng generate` runs built-in schematics; `ng add` runs a library's",
              "`ng update` runs migration schematics across major versions",
              "Custom schematics enforce team architecture patterns"
            ],
            "detail": "Changes are atomic — only written if the whole schematic succeeds. Schematics can do AST manipulation (add imports, edit decorators).",
            "followUps": [
              "How would you write a custom team schematic?",
              "ng add vs ng install?",
              "How do update schematics differ from a package bump?"
            ],
            "redFlags": [
              "Not knowing what runs behind ng generate/ng add",
              "Updating Angular without ng update (misses migrations)"
            ]
          }
        },
        {
          "id": "angular-advanced-012",
          "q": "How would you optimise a slow Angular application? What tools and techniques would you use?",
          "level": "senior",
          "type": "scenario",
          "freq": "high",
          "tags": ["performance", "optimization", "OnPush", "lazy loading", "bundle", "profiling"],
          "answer": {
            "oneLiner": "Profile first (Angular DevTools, bundle analyser), then apply OnPush/signals, lazy load and @defer, use track in loops, prefer pure pipes over template methods, and trim the bundle.",
            "keyPoints": [
              "Profile first — Angular DevTools, Chrome Performance, source-map-explorer",
              "Change detection — OnPush + immutability, or signals for zoneless",
              "Bundle — lazy routes, @defer, avoid huge dependencies",
              "Templates — track in loops, pure pipes, no function calls in templates"
            ],
            "detail": "Manage subscriptions with takeUntilDestroyed/async pipe. For Core Web Vitals, add SSR (`ng add @angular/ssr`) for LCP and @defer to cut TBT.",
            "code": {
              "lang": "html",
              "snippet": "<!-- Bad: function called every CD cycle -->\n<li *ngFor=\"let i of getItems()\">{{ formatName(i) }}</li>\n\n<!-- Good: signal or pure pipe -->\n@for (i of items(); track i.id) { <li>{{ i.name | titlecase }}</li> }",
              "note": "Avoid method calls in templates; use signals/pure pipes with track"
            },
            "followUps": [
              "How to find which component causes excessive CD?",
              "Method in template vs pure pipe — impact?",
              "How does SSR improve performance metrics?"
            ],
            "redFlags": [
              "Optimising without profiling first",
              "Applying OnPush everywhere without immutability",
              "Not knowing DevTools or bundle analyser"
            ]
          }
        }
      ]
    }
  ]
}
