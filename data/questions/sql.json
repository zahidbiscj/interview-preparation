{
  "topicId": "sql",
  "topicName": "SQL Server",
  "subtopics": [
    {
      "subtopicId": "queries",
      "subtopicName": "Queries & Indexing",
      "order": 1,
      "questions": [
        {
          "id": "sql-queries-001",
          "q": "What is the difference between DELETE, TRUNCATE, and DROP?",
          "level": "junior",
          "type": "comparison",
          "freq": "high",
          "tags": ["delete", "truncate", "drop", "ddl", "dml"],
          "answer": {
            "oneLiner": "DELETE removes rows one-by-one (logged, can use WHERE); TRUNCATE wipes all rows by deallocating pages (fast, resets identity); DROP removes the whole table object.",
            "keyPoints": [
              "DELETE: DML, per-row logging, fires triggers, supports WHERE",
              "TRUNCATE: DDL, minimal logging, no WHERE, resets IDENTITY",
              "DROP: removes table structure, data, indexes, constraints",
              "Both DELETE and TRUNCATE roll back inside an explicit transaction"
            ],
            "code": {
              "lang": "sql",
              "snippet": "-- DELETE: partial removal, triggers fire, per-row log\nDELETE FROM Orders WHERE OrderDate < '2020-01-01';\n\n-- TRUNCATE: all rows gone, minimal log, identity resets, no WHERE\nTRUNCATE TABLE StagingOrders;\n\n-- DROP: table no longer exists\nDROP TABLE StagingOrders;",
              "note": "DELETE supports WHERE; TRUNCATE does not"
            },
            "analogy": "DELETE tears pages out one by one; TRUNCATE recycles all pages but keeps the cover; DROP shreds the whole book.",
            "followUps": [
              "Can TRUNCATE be rolled back?",
              "Why doesn't TRUNCATE fire triggers?",
              "What happens to the IDENTITY column after each?"
            ],
            "redFlags": [
              "Saying TRUNCATE can never be rolled back",
              "Confusing TRUNCATE and DROP",
              "Forgetting DELETE fires triggers"
            ]
          }
        },
        {
          "id": "sql-queries-002",
          "q": "Explain INNER JOIN, LEFT JOIN, RIGHT JOIN, and FULL OUTER JOIN with examples.",
          "level": "junior",
          "type": "conceptual",
          "freq": "high",
          "tags": ["joins", "inner join", "left join", "right join", "full outer join"],
          "answer": {
            "oneLiner": "INNER returns only matches; LEFT returns all left rows plus matches (NULLs otherwise); RIGHT is the mirror; FULL OUTER returns all rows from both sides.",
            "keyPoints": [
              "INNER: only rows matching the ON condition in both tables",
              "LEFT: all left rows, NULLs where no right match",
              "RIGHT: all right rows, NULLs where no left match",
              "FULL OUTER: all rows from both, NULLs where no match",
              "LEFT + WHERE right.key IS NULL = anti-join"
            ],
            "code": {
              "lang": "sql",
              "snippet": "-- Sample tables: Customers(CustomerId, Name), Orders(OrderId, CustomerId, Total)\n\n-- INNER: only customers who have orders\nSELECT c.Name, o.Total\nFROM Customers c\nINNER JOIN Orders o ON c.CustomerId = o.CustomerId;\n\n-- LEFT: all customers, NULL Total where no order exists\nSELECT c.Name, o.Total\nFROM Customers c\nLEFT JOIN Orders o ON c.CustomerId = o.CustomerId;\n\n-- Anti-join: customers with NO orders\nSELECT c.Name\nFROM Customers c\nLEFT JOIN Orders o ON c.CustomerId = o.CustomerId\nWHERE o.CustomerId IS NULL;\n\n-- FULL OUTER: all rows from both tables\nSELECT c.Name, o.Total\nFROM Customers c\nFULL OUTER JOIN Orders o ON c.CustomerId = o.CustomerId;",
              "note": "Anti-join pattern (LEFT + WHERE IS NULL) is a classic interview question"
            },
            "analogy": "Two Venn circles: INNER is the overlap, LEFT/RIGHT is one whole circle, FULL OUTER is both circles combined.",
            "followUps": [
              "What is a CROSS JOIN and when would you use it?",
              "How does a SELF JOIN work?",
              "JOIN vs subquery performance?"
            ],
            "redFlags": [
              "Confusing LEFT and RIGHT semantics",
              "Not knowing the anti-join pattern",
              "Thinking FULL OUTER duplicates rows"
            ]
          }
        },
        {
          "id": "sql-queries-003",
          "q": "How do you optimize a slow SQL query, and how do you read an execution plan?",
          "level": "senior",
          "type": "scenario",
          "freq": "high",
          "tags": ["performance", "execution plan", "optimization", "index", "statistics"],
          "answer": {
            "oneLiner": "Start with the execution plan: look for scans, thick arrows, and Key Lookups, then fix with proper indexes, SARGable predicates, and updated statistics. Always measure with SET STATISTICS IO before and after.",
            "keyPoints": [
              "Baseline with SET STATISTICS IO/TIME ON (logical reads = I/O cost)",
              "Read graphical plans right-to-left; thick arrows = many rows",
              "Watch for Table Scan, Key Lookup, Sort, expensive Hash Match",
              "Fix non-SARGable predicates, SELECT *, implicit conversions",
              "Update stale statistics; missing-index hints are a hint, not gospel"
            ],
            "detail": "Key Lookup means a non-clustered index seek had to go back to the clustered index for extra columns; fix with a covering index (INCLUDE). Make predicates SARGable by keeping functions off indexed columns.",
            "code": {
              "lang": "sql",
              "snippet": "-- Baseline I/O measurement\nSET STATISTICS IO ON;\nSET STATISTICS TIME ON;\n\n-- Non-SARGable (BAD) -- function prevents index seek\nSELECT * FROM Orders WHERE YEAR(OrderDate) = 2024;\n\n-- SARGable (GOOD) -- allows index seek\nSELECT OrderId, CustomerId, Total\nFROM Orders\nWHERE OrderDate >= '2024-01-01' AND OrderDate < '2025-01-01';\n\n-- Covering index: seek on CustomerId, include Total to avoid Key Lookup\nCREATE NONCLUSTERED INDEX IX_Orders_CustomerId_Inc\n  ON Orders (CustomerId)\n  INCLUDE (Total, OrderDate);",
              "note": "Always benchmark with SET STATISTICS IO to confirm improvement"
            },
            "followUps": [
              "What is parameter sniffing and how do you deal with it?",
              "When would you use OPTION (RECOMPILE)?",
              "How does Query Store help with regressions?"
            ],
            "redFlags": [
              "Adding indexes without reading the plan first",
              "Using NOLOCK as a default performance fix",
              "Not knowing what a Key Lookup is"
            ]
          }
        },
        {
          "id": "sql-queries-004",
          "q": "What is a CTE (Common Table Expression) and when would you use one?",
          "level": "mid",
          "type": "conceptual",
          "freq": "high",
          "tags": ["cte", "with", "recursive", "readability", "subquery"],
          "answer": {
            "oneLiner": "A CTE is a named temporary result set defined with WITH that exists only for the next single statement; it improves readability and enables recursion for hierarchies.",
            "keyPoints": [
              "Syntax: WITH Name AS (SELECT ...) then a SELECT/INSERT/UPDATE/DELETE",
              "Scoped to one statement; not a physical temp table",
              "Recursive CTEs: anchor UNION ALL recursive member, for hierarchies",
              "Default recursion limit 100; override with OPTION (MAXRECURSION n)"
            ],
            "detail": "A non-recursive CTE is usually inlined by the optimizer, not materialized, so referencing it many times can re-execute it; use a #temp table if that's expensive.",
            "code": {
              "lang": "sql",
              "snippet": "-- Non-recursive: top customers by order value\nWITH CustomerTotals AS (\n    SELECT CustomerId, SUM(Total) AS TotalSpent\n    FROM Orders\n    GROUP BY CustomerId\n)\nSELECT c.Name, ct.TotalSpent\nFROM CustomerTotals ct\nJOIN Customers c ON c.CustomerId = ct.CustomerId\nORDER BY ct.TotalSpent DESC;\n\n-- Recursive: org-chart hierarchy\nWITH OrgChart AS (\n    -- Anchor: CEO (no manager)\n    SELECT EmployeeId, Name, ManagerId, 0 AS Level\n    FROM Employees\n    WHERE ManagerId IS NULL\n\n    UNION ALL\n\n    -- Recursive: reports\n    SELECT e.EmployeeId, e.Name, e.ManagerId, oc.Level + 1\n    FROM Employees e\n    INNER JOIN OrgChart oc ON e.ManagerId = oc.EmployeeId\n)\nSELECT * FROM OrgChart\nOPTION (MAXRECURSION 50);",
              "note": "Recursive CTEs require UNION ALL — UNION is not allowed in the recursive member"
            },
            "analogy": "A CTE is like naming an intermediate calculation on a whiteboard, then erasing it when done.",
            "followUps": [
              "CTE vs temp table?",
              "Can a CTE be indexed?",
              "What if a recursive CTE never terminates?"
            ],
            "redFlags": [
              "Thinking a CTE is persisted like a temp table",
              "Not knowing about recursive CTEs",
              "Claiming CTEs always improve performance"
            ]
          }
        },
        {
          "id": "sql-queries-005",
          "q": "What is a temporary table? What is the difference between #temp, ##temp, and a table variable (@table)?",
          "level": "mid",
          "type": "comparison",
          "freq": "high",
          "tags": ["temp table", "table variable", "tempdb", "scope", "local temp", "global temp"],
          "answer": {
            "oneLiner": "#temp is session-scoped in tempdb; ##temp is global across all sessions; @table is batch-scoped, has no real statistics, and is not undone by a transaction rollback.",
            "keyPoints": [
              "#temp: tempdb, session-scoped, supports indexes/stats, rolls back",
              "##temp: tempdb, visible to all sessions until creator disconnects",
              "@table: batch/proc-scoped, no stats (optimizer assumes 1 row), rollback-immune",
              "Small sets: table variable; larger sets: #temp (better stats)"
            ],
            "detail": "Because table variables have no statistics, the optimizer estimates 1 row and can pick bad plans on large data. Their rollback immunity makes them handy for audit logging inside TRY/CATCH.",
            "code": {
              "lang": "sql",
              "snippet": "-- Local temp table\nCREATE TABLE #RecentOrders (\n    OrderId   INT,\n    Total     DECIMAL(18,2)\n);\nINSERT INTO #RecentOrders\n    SELECT OrderId, Total FROM Orders WHERE OrderDate > DATEADD(DAY,-30,GETDATE());\nSELECT * FROM #RecentOrders;\nDROP TABLE IF EXISTS #RecentOrders;\n\n-- Table variable (no indexes except PK/UNIQUE at declaration)\nDECLARE @TopCustomers TABLE (\n    CustomerId INT PRIMARY KEY,\n    TotalSpent DECIMAL(18,2)\n);\nINSERT INTO @TopCustomers\n    SELECT TOP 10 CustomerId, SUM(Total) FROM Orders GROUP BY CustomerId ORDER BY SUM(Total) DESC;\nSELECT * FROM @TopCustomers;",
              "note": "Table variables cannot have CREATE INDEX added after declaration; temp tables can"
            },
            "followUps": [
              "Can a temp table be accessed from another connection?",
              "Why don't table variables roll back?",
              "When choose a CTE over a temp table?"
            ],
            "redFlags": [
              "Saying table variables behave just like temp tables",
              "Not knowing ##temp is visible across sessions",
              "Claiming table variables are always faster"
            ]
          }
        },
        {
          "id": "sql-queries-006",
          "q": "What is a trigger? How do you create and use one in SQL Server?",
          "level": "mid",
          "type": "conceptual",
          "freq": "medium",
          "tags": ["trigger", "dml trigger", "ddl trigger", "inserted", "deleted", "after", "instead of"],
          "answer": {
            "oneLiner": "A trigger is a special stored procedure that fires automatically on DML or DDL events, using the virtual INSERTED and DELETED tables to see the new and old row states.",
            "keyPoints": [
              "AFTER (default): fires after the DML; you can still ROLLBACK",
              "INSTEAD OF: replaces the DML; used to make views updatable",
              "INSERTED = new rows, DELETED = old rows; both populated on UPDATE",
              "Runs in the same transaction; always handle multi-row cases"
            ],
            "detail": "A ROLLBACK inside a trigger aborts the whole calling transaction. Keep triggers minimal — they are invisible to app developers and hard to debug.",
            "code": {
              "lang": "sql",
              "snippet": "-- AFTER INSERT trigger: log new orders to an audit table\nCREATE OR ALTER TRIGGER trg_Orders_AfterInsert\nON Orders\nAFTER INSERT\nAS\nBEGIN\n    SET NOCOUNT ON;\n    INSERT INTO OrderAudit (OrderId, CustomerId, Total, AuditAction, AuditDate)\n    SELECT OrderId, CustomerId, Total, 'INSERT', GETUTCDATE()\n    FROM INSERTED;  -- INSERTED can have multiple rows\nEND;\n\n-- INSTEAD OF UPDATE trigger on a view\nCREATE OR ALTER TRIGGER trg_vCustomerOrders_InsteadOfUpdate\nON vCustomerOrders\nINSTEAD OF UPDATE\nAS\nBEGIN\n    SET NOCOUNT ON;\n    UPDATE o SET o.Total = i.Total\n    FROM Orders o\n    INNER JOIN INSERTED i ON o.OrderId = i.OrderId;\nEND;",
              "note": "Always handle multi-row: INSERTED is a table, not a single row"
            },
            "followUps": [
              "AFTER vs INSTEAD OF triggers?",
              "Can triggers be recursive/nested?",
              "How would you audit changes without a trigger?"
            ],
            "redFlags": [
              "Assuming INSERTED/DELETED hold exactly one row",
              "Not knowing a ROLLBACK in a trigger rolls back the caller",
              "Overusing triggers for business logic"
            ]
          }
        },
        {
          "id": "sql-queries-007",
          "q": "What is an index? Explain the difference between a clustered and a non-clustered index.",
          "level": "junior",
          "type": "conceptual",
          "freq": "high",
          "tags": ["index", "clustered index", "non-clustered index", "b-tree", "performance"],
          "answer": {
            "oneLiner": "An index is a B-tree that speeds up lookups. A clustered index physically sorts the table by its key (one per table); a non-clustered index is a separate structure with the key plus a pointer back to the row (up to 999).",
            "keyPoints": [
              "Table with no clustered index = heap (unordered)",
              "Clustered leaf nodes ARE the data rows",
              "Non-clustered leaf = key + row locator (clustered key or RID)",
              "Key Lookup happens when extra columns are needed; fix with covering index",
              "Indexes speed reads but slow writes"
            ],
            "code": {
              "lang": "sql",
              "snippet": "-- Default: PRIMARY KEY creates a clustered index\nCREATE TABLE Orders (\n    OrderId   INT IDENTITY PRIMARY KEY,  -- clustered by default\n    CustomerId INT NOT NULL,\n    OrderDate  DATE NOT NULL,\n    Total      DECIMAL(18,2)\n);\n\n-- Non-clustered index on CustomerId\nCREATE NONCLUSTERED INDEX IX_Orders_CustomerId\n    ON Orders (CustomerId);\n\n-- Covering index: include Total to avoid Key Lookup for this common query\nCREATE NONCLUSTERED INDEX IX_Orders_Customer_Covering\n    ON Orders (CustomerId)\n    INCLUDE (OrderDate, Total);\n\n-- Explicit clustered index (different column)\nCREATE CLUSTERED INDEX CIX_Orders_OrderDate\n    ON Orders (OrderDate);  -- table physically sorted by date",
              "note": "Only one clustered index per table; choose the key wisely as it affects all non-clustered indexes"
            },
            "analogy": "Clustered = phone book sorted by last name (data itself is ordered). Non-clustered = the index at the back of a book pointing to page numbers.",
            "followUps": [
              "What is a covering index?",
              "Why are GUIDs poor clustered keys?",
              "What is index fragmentation and how do you fix it?"
            ],
            "redFlags": [
              "Saying you can have multiple clustered indexes",
              "Not knowing what a Key Lookup is",
              "Ignoring the write overhead of indexes"
            ]
          }
        },
        {
          "id": "sql-queries-008",
          "q": "Write a query to find the Nth highest salary from an Employees table.",
          "level": "mid",
          "type": "code",
          "freq": "high",
          "tags": ["nth highest", "row_number", "dense_rank", "window functions", "salary"],
          "answer": {
            "oneLiner": "Use DENSE_RANK() ordered by salary descending in a CTE, then filter WHERE rank = N — it handles ties correctly.",
            "keyPoints": [
              "DENSE_RANK is preferred: ties get the same rank, no gaps",
              "ROW_NUMBER gives unique numbers — wrong when salaries tie",
              "Window functions can't go in WHERE — wrap in a CTE/subquery",
              "Alternative: correlated subquery with COUNT(DISTINCT) — slower"
            ],
            "code": {
              "lang": "sql",
              "snippet": "-- Method 1: DENSE_RANK in a CTE (preferred)\nWITH RankedSalaries AS (\n    SELECT\n        EmployeeId,\n        Name,\n        Salary,\n        DENSE_RANK() OVER (ORDER BY Salary DESC) AS SalaryRank\n    FROM Employees\n)\nSELECT EmployeeId, Name, Salary\nFROM RankedSalaries\nWHERE SalaryRank = 3;  -- Change 3 to any N\n\n-- Method 2: ROW_NUMBER (wrong for ties, but interviewer may ask for the distinction)\nWITH Numbered AS (\n    SELECT EmployeeId, Name, Salary,\n           ROW_NUMBER() OVER (ORDER BY Salary DESC) AS RowNum\n    FROM Employees\n)\nSELECT * FROM Numbered WHERE RowNum = 3;\n\n-- Method 3: Classic correlated subquery (for comparison only)\nSELECT MIN(Salary)\nFROM (\n    SELECT DISTINCT TOP 3 Salary\n    FROM Employees\n    ORDER BY Salary DESC\n) AS Top3;",
              "note": "Always clarify tie-handling with the interviewer — DENSE_RANK is the safe default"
            },
            "followUps": [
              "Difference between RANK, DENSE_RANK, ROW_NUMBER?",
              "Nth highest salary per department?",
              "Can you do it without a window function?"
            ],
            "redFlags": [
              "Using ROW_NUMBER without noting it differs on ties",
              "Unable to write it with a window function",
              "Forgetting window functions can't go in WHERE"
            ]
          }
        },
        {
          "id": "sql-queries-009",
          "q": "What is the difference between WHERE and HAVING, and when do you use GROUP BY?",
          "level": "junior",
          "type": "comparison",
          "freq": "high",
          "tags": ["where", "having", "group by", "aggregation", "filter"],
          "answer": {
            "oneLiner": "WHERE filters rows before aggregation; HAVING filters groups after aggregation; GROUP BY collapses rows sharing a key so aggregates can be applied.",
            "keyPoints": [
              "WHERE runs before GROUP BY — no aggregates allowed",
              "HAVING runs after GROUP BY — aggregates allowed",
              "Order: FROM → WHERE → GROUP BY → HAVING → SELECT → ORDER BY",
              "Filter with WHERE first to cut rows before grouping"
            ],
            "code": {
              "lang": "sql",
              "snippet": "-- Customers who placed more than 5 orders with a total spend > $1000\nSELECT\n    c.Name,\n    COUNT(o.OrderId)      AS OrderCount,\n    SUM(o.Total)          AS TotalSpent\nFROM Customers c\nINNER JOIN Orders o ON c.CustomerId = o.CustomerId\nWHERE o.OrderDate >= '2023-01-01'       -- WHERE: filter rows first\nGROUP BY c.CustomerId, c.Name           -- GROUP BY: collapse into groups\nHAVING COUNT(o.OrderId) > 5            -- HAVING: filter groups by aggregate\n   AND SUM(o.Total) > 1000\nORDER BY TotalSpent DESC;",
              "note": "The WHERE clause here removes old orders BEFORE grouping — more efficient than filtering after"
            },
            "followUps": [
              "Can you use a SELECT alias in WHERE?",
              "What does HAVING without GROUP BY do?",
              "COUNT(*) vs COUNT(column)?"
            ],
            "redFlags": [
              "Putting aggregates in WHERE instead of HAVING",
              "Not knowing the logical processing order",
              "Omitting a non-aggregate column from GROUP BY"
            ]
          }
        },
        {
          "id": "sql-queries-010",
          "q": "Explain window functions — ROW_NUMBER, RANK, and DENSE_RANK — with examples.",
          "level": "mid",
          "type": "conceptual",
          "freq": "high",
          "tags": ["window functions", "row_number", "rank", "dense_rank", "over", "partition by"],
          "answer": {
            "oneLiner": "Window functions compute across related rows without collapsing them. ROW_NUMBER is always unique (1,2,3), RANK skips after ties (1,1,3), DENSE_RANK never skips (1,1,2).",
            "keyPoints": [
              "Unlike GROUP BY, rows are NOT collapsed",
              "OVER defines the window: PARTITION BY resets per group, ORDER BY sorts within",
              "ROW_NUMBER unique; RANK gaps after ties; DENSE_RANK no gaps",
              "Also: LAG/LEAD, SUM/AVG OVER (running totals), NTILE"
            ],
            "detail": "Common use: de-dupe with ROW_NUMBER() OVER (PARTITION BY key ORDER BY date DESC) and keep rn = 1.",
            "code": {
              "lang": "sql",
              "snippet": "SELECT\n    Name,\n    Department,\n    Salary,\n    ROW_NUMBER()  OVER (PARTITION BY Department ORDER BY Salary DESC) AS RowNum,\n    RANK()        OVER (PARTITION BY Department ORDER BY Salary DESC) AS Rnk,\n    DENSE_RANK()  OVER (PARTITION BY Department ORDER BY Salary DESC) AS DenseRnk\nFROM Employees;\n\n-- Result for Engineering dept with salaries 90k, 85k, 85k, 70k:\n-- RowNum: 1, 2, 3, 4\n-- Rank:   1, 2, 2, 4   (skips 3)\n-- DenseRnk: 1, 2, 2, 3 (no skip)\n\n-- De-duplicate: keep latest record per CustomerId\nWITH Deduped AS (\n    SELECT *, ROW_NUMBER() OVER (PARTITION BY CustomerId ORDER BY UpdatedAt DESC) AS rn\n    FROM CustomerEvents\n)\nSELECT * FROM Deduped WHERE rn = 1;",
              "note": "De-duplication with ROW_NUMBER is one of the most common real-world uses"
            },
            "followUps": [
              "ROWS vs RANGE in a window frame?",
              "LAG() vs a self-join?",
              "Can window functions be used in WHERE or GROUP BY?"
            ],
            "redFlags": [
              "Confusing RANK and DENSE_RANK gap behavior",
              "Not knowing window functions keep all rows",
              "Trying to use them in WHERE"
            ]
          }
        },
        {
          "id": "sql-queries-011",
          "q": "What is a covering index and how does it eliminate a Key Lookup?",
          "level": "senior",
          "type": "conceptual",
          "freq": "high",
          "tags": ["covering index", "include", "key lookup", "non-clustered", "performance"],
          "answer": {
            "oneLiner": "A covering index contains every column a query needs — key columns plus INCLUDE columns — so the query is answered entirely from the index without a Key Lookup to the base table.",
            "keyPoints": [
              "Key Lookup = extra I/O per row to fetch columns from the clustered index",
              "INCLUDE adds non-key columns at the leaf level only",
              "Key columns: seek/filter/sort; INCLUDE: coverage only",
              "Trade-off: bigger index, slower writes on included columns"
            ],
            "detail": "Put a column in the key if you seek, range-scan, or sort on it; use INCLUDE if it's only needed in SELECT. Check sys.dm_db_missing_index_details for suggestions, but validate against the real workload.",
            "code": {
              "lang": "sql",
              "snippet": "-- Query that causes a Key Lookup on IX_Orders_CustomerId\nSELECT OrderId, OrderDate, Total\nFROM Orders\nWHERE CustomerId = 42;\n-- Execution plan shows: Index Seek (IX_Orders_CustomerId) -> Key Lookup\n\n-- Fix: covering index\nCREATE NONCLUSTERED INDEX IX_Orders_CustomerId_Covering\n    ON Orders (CustomerId)          -- key: seek happens here\n    INCLUDE (OrderDate, Total);     -- leaf only: eliminates Key Lookup\n\n-- Find missing index suggestions\nSELECT\n    mid.statement AS TableName,\n    mid.equality_columns,\n    mid.include_columns,\n    migs.avg_user_impact\nFROM sys.dm_db_missing_index_details mid\nJOIN sys.dm_db_missing_index_groups mig ON mid.index_handle = mig.index_handle\nJOIN sys.dm_db_missing_index_group_stats migs ON mig.index_group_handle = migs.group_handle\nORDER BY migs.avg_user_impact DESC;",
              "note": "INCLUDE columns are stored only at leaf level — they don't affect seek efficiency but eliminate Key Lookups"
            },
            "followUps": [
              "Key column vs INCLUDE column?",
              "How many indexes on a write-heavy table?",
              "What is index intersection?"
            ],
            "redFlags": [
              "Not knowing what a Key Lookup is",
              "Putting every column in the key instead of INCLUDE",
              "Blindly applying every missing index suggestion"
            ]
          }
        },
        {
          "id": "sql-queries-012",
          "q": "What causes a table scan vs an index seek? When does the optimizer choose one over the other?",
          "level": "senior",
          "type": "conceptual",
          "freq": "high",
          "tags": ["table scan", "index seek", "optimizer", "statistics", "selectivity", "sargable"],
          "answer": {
            "oneLiner": "The optimizer scans when there's no suitable index, the predicate is non-SARGable, selectivity is low, or stats are stale; it seeks for high-selectivity SARGable predicates.",
            "keyPoints": [
              "Seek: navigates the B-tree; good when few rows match",
              "Scan: reads every row; chosen for low selectivity or no index",
              "SARGable = optimizer can seek; functions on columns / leading % / implicit conversions break it",
              "Optimizer uses statistics; stale stats cause bad estimates",
              "Above ~1-3% of rows, a scan can beat seek + Key Lookup"
            ],
            "detail": "Implicit type conversion (VARCHAR column vs NVARCHAR parameter) converts every row and forces a scan — it shows as a warning in the plan. Keep statistics updated.",
            "code": {
              "lang": "sql",
              "snippet": "-- Non-SARGable: function on column causes scan\nSELECT * FROM Orders WHERE YEAR(OrderDate) = 2024;  -- BAD\n\n-- SARGable equivalent: allows index seek on OrderDate\nSELECT * FROM Orders\nWHERE OrderDate >= '2024-01-01' AND OrderDate < '2025-01-01';  -- GOOD\n\n-- Implicit conversion (scan warning in plan if col is VARCHAR)\nDECLARE @id NVARCHAR(10) = N'123';\nSELECT * FROM Orders WHERE OrderId = @id;  -- type mismatch may prevent seek\n\n-- Check column statistics\nDBCC SHOW_STATISTICS ('Orders', 'IX_Orders_CustomerId');\n\n-- Update stale statistics\nUPDATE STATISTICS Orders WITH FULLSCAN;",
              "note": "Green warnings in the execution plan often indicate implicit conversions causing index scans"
            },
            "followUps": [
              "What is parameter sniffing?",
              "How (and when not) to force an index seek?",
              "What is a filtered index?"
            ],
            "redFlags": [
              "Not knowing what SARGable means",
              "Thinking an index always means a seek",
              "Ignoring statistics as a cause of bad plans"
            ]
          }
        },
        {
          "id": "sql-queries-013",
          "q": "What is the difference between UNION and UNION ALL?",
          "level": "junior",
          "type": "comparison",
          "freq": "medium",
          "tags": ["union", "union all", "distinct", "duplicates", "set operations"],
          "answer": {
            "oneLiner": "UNION removes duplicates via a sort/hash step; UNION ALL keeps everything and is faster because there's no dedup. Default to UNION ALL unless you need dedup.",
            "keyPoints": [
              "Both need matching column count and compatible types",
              "UNION = implicit DISTINCT (extra blocking step, slower)",
              "UNION ALL keeps duplicates, no extra work",
              "ORDER BY only once, at the very end"
            ],
            "code": {
              "lang": "sql",
              "snippet": "-- UNION: deduplicates; slower\nSELECT CustomerId FROM ActiveCustomers\nUNION\nSELECT CustomerId FROM ArchivedCustomers;\n\n-- UNION ALL: keeps duplicates; faster — use when sets are disjoint\nSELECT OrderId, Total, 'Online' AS Channel FROM OnlineOrders\nUNION ALL\nSELECT OrderId, Total, 'Store'  AS Channel FROM StoreOrders\nORDER BY Total DESC;  -- ORDER BY goes at the end only",
              "note": "Default to UNION ALL unless you specifically need deduplication"
            },
            "followUps": [
              "UNION vs JOIN?",
              "How do INTERSECT and EXCEPT work?",
              "When does UNION cause a performance issue?"
            ],
            "redFlags": [
              "Defaulting to UNION when UNION ALL suffices",
              "Not knowing why UNION is slower",
              "Putting ORDER BY mid-chain"
            ]
          }
        },
        {
          "id": "sql-queries-014",
          "q": "What is the difference between a stored procedure and inline (ad-hoc) SQL?",
          "level": "mid",
          "type": "comparison",
          "freq": "medium",
          "tags": ["stored procedure", "inline sql", "sql injection", "plan caching", "security"],
          "answer": {
            "oneLiner": "Stored procedures are named, cached T-SQL objects with permission control and injection safety; parameterized inline SQL (sp_executesql) gets the same caching and safety, while string-concatenated SQL is vulnerable.",
            "keyPoints": [
              "Stored procs: plan cached on first run, reused afterward",
              "Parameterized inline SQL caches and is injection-safe too",
              "GRANT EXECUTE without granting table access = security boundary",
              "String-concatenated SQL is the real injection risk, not inline per se"
            ],
            "detail": "Both stored procs and sp_executesql are subject to parameter sniffing — the plan is built from the first parameters seen. Mitigate with OPTION (RECOMPILE), OPTIMIZE FOR, or Query Store.",
            "code": {
              "lang": "sql",
              "snippet": "-- Stored procedure with error handling\nCREATE OR ALTER PROCEDURE usp_GetCustomerOrders\n    @CustomerId INT,\n    @StartDate  DATE = NULL\nAS\nBEGIN\n    SET NOCOUNT ON;\n    SELECT o.OrderId, o.Total, o.OrderDate\n    FROM Orders o\n    WHERE o.CustomerId = @CustomerId\n      AND (@StartDate IS NULL OR o.OrderDate >= @StartDate)\n    ORDER BY o.OrderDate DESC;\nEND;\n\n-- Execute\nEXEC usp_GetCustomerOrders @CustomerId = 42, @StartDate = '2024-01-01';\n\n-- Parameterized inline SQL (also safe, also cached)\nEXEC sp_executesql\n    N'SELECT * FROM Orders WHERE CustomerId = @cid',\n    N'@cid INT',\n    @cid = 42;",
              "note": "sp_executesql with parameters is as safe as a stored procedure for injection purposes"
            },
            "followUps": [
              "What is parameter sniffing and how do you deal with it?",
              "Inline table-valued function vs stored procedure?",
              "How does EF Core handle parameterization?"
            ],
            "redFlags": [
              "Thinking stored procs are always faster than parameterized SQL",
              "Not knowing about parameter sniffing",
              "Thinking any stored proc prevents injection (not if it concatenates strings)"
            ]
          }
        }
      ]
    },
    {
      "subtopicId": "transactions",
      "subtopicName": "Transactions & Design",
      "order": 2,
      "questions": [
        {
          "id": "sql-transactions-001",
          "q": "What are ACID properties? Explain each in the context of SQL Server transactions.",
          "level": "mid",
          "type": "conceptual",
          "freq": "high",
          "tags": ["acid", "atomicity", "consistency", "isolation", "durability", "transactions"],
          "answer": {
            "oneLiner": "ACID = Atomicity (all-or-nothing), Consistency (always valid state), Isolation (concurrent txns don't interfere), Durability (committed data survives crashes), backed by write-ahead logging and locking.",
            "keyPoints": [
              "Atomicity: all statements commit together or all roll back",
              "Consistency: constraints/triggers keep the DB in a valid state",
              "Isolation: controlled by isolation levels and locks",
              "Durability: committed log is hardened to disk (Write-Ahead Logging)"
            ],
            "detail": "SET XACT_ABORT ON makes any runtime error roll back the whole transaction, not just the failing statement — use it with TRY/CATCH in stored procedures.",
            "code": {
              "lang": "sql",
              "snippet": "-- Atomic bank transfer: both debit and credit must succeed together\nSET XACT_ABORT ON;\nBEGIN TRY\n    BEGIN TRANSACTION;\n\n    UPDATE Accounts SET Balance = Balance - 500 WHERE AccountId = 1;\n    UPDATE Accounts SET Balance = Balance + 500 WHERE AccountId = 2;\n\n    -- Consistency check\n    IF (SELECT Balance FROM Accounts WHERE AccountId = 1) < 0\n        THROW 50001, 'Insufficient funds', 1;\n\n    COMMIT TRANSACTION;\nEND TRY\nBEGIN CATCH\n    IF @@TRANCOUNT > 0 ROLLBACK TRANSACTION;\n    THROW;  -- re-raise the error\nEND CATCH;",
              "note": "XACT_ABORT ON ensures the transaction rolls back on any error, not just the failing statement"
            },
            "analogy": "Like an ATM withdrawal: you get cash and the balance drops together (Atomic), account stays valid (Consistent), others aren't affected mid-way (Isolated), and the record is permanent once confirmed (Durable).",
            "followUps": [
              "ROLLBACK vs SAVEPOINT?",
              "How is durability ensured during a power failure?",
              "What is XACT_ABORT and why use it?"
            ],
            "redFlags": [
              "Confusing Isolation with the other properties",
              "Not knowing about the Write-Ahead Log",
              "Omitting TRY/CATCH and XACT_ABORT"
            ]
          }
        },
        {
          "id": "sql-transactions-002",
          "q": "What are SQL Server isolation levels, and what concurrency problems does each prevent?",
          "level": "senior",
          "type": "conceptual",
          "freq": "high",
          "tags": ["isolation levels", "dirty read", "phantom read", "non-repeatable read", "snapshot", "read committed"],
          "answer": {
            "oneLiner": "Five levels — Read Uncommitted, Read Committed (default), Repeatable Read, Serializable, Snapshot — trading concurrency against protection from dirty reads, non-repeatable reads, and phantoms.",
            "keyPoints": [
              "Dirty read: reading uncommitted data (only at Read Uncommitted)",
              "Non-repeatable read: same row re-read differs (fixed at Repeatable Read+)",
              "Phantom: range re-query returns new rows (fixed at Serializable/Snapshot)",
              "RCSI: readers see last committed version, no read locks — recommended for OLTP",
              "Snapshot: whole txn sees a point-in-time view; detects write conflicts"
            ],
            "detail": "RCSI (READ_COMMITTED_SNAPSHOT ON) makes readers not block writers without app changes. Snapshot Isolation versions at transaction start and throws a conflict error on concurrent updates.",
            "code": {
              "lang": "sql",
              "snippet": "-- Set isolation level for the current session\nSET TRANSACTION ISOLATION LEVEL READ COMMITTED;  -- default\nSET TRANSACTION ISOLATION LEVEL READ UNCOMMITTED; -- dirty reads allowed\nSET TRANSACTION ISOLATION LEVEL REPEATABLE READ;\nSET TRANSACTION ISOLATION LEVEL SERIALIZABLE;\nSET TRANSACTION ISOLATION LEVEL SNAPSHOT;\n\n-- Enable RCSI at database level (eliminates read-write blocking)\nALTER DATABASE AdventureWorks\n    SET READ_COMMITTED_SNAPSHOT ON WITH ROLLBACK IMMEDIATE;\n\n-- Query hint equivalent of READ UNCOMMITTED (avoid in production)\nSELECT * FROM Orders WITH (NOLOCK);",
              "note": "Enable RCSI on Azure SQL / SQL Server to dramatically reduce blocking without changing application code"
            },
            "followUps": [
              "RCSI vs Snapshot Isolation?",
              "Why is NOLOCK dangerous?",
              "How does Snapshot detect write conflicts?"
            ],
            "redFlags": [
              "Recommending NOLOCK without explaining the dangers",
              "Not knowing dirty/non-repeatable/phantom reads",
              "Confusing RCSI with Snapshot Isolation"
            ]
          }
        },
        {
          "id": "sql-transactions-003",
          "q": "What causes a deadlock in SQL Server and how do you prevent or resolve it?",
          "level": "senior",
          "type": "scenario",
          "freq": "high",
          "tags": ["deadlock", "locking", "transaction", "deadlock graph", "prevention"],
          "answer": {
            "oneLiner": "A deadlock is a circular wait where two transactions each hold a lock the other needs; SQL Server auto-kills the cheapest victim (error 1205). Prevent with consistent lock order, short transactions, and good indexes.",
            "keyPoints": [
              "Lock monitor detects the cycle; victim gets error 1205",
              "Always access tables in the same order across transactions",
              "Keep transactions short — commit fast",
              "Good indexes shrink lock scope; RCSI removes reader-writer deadlocks",
              "App should catch 1205 and retry"
            ],
            "detail": "The system_health Extended Events session captures deadlock graphs by default; open the .xdl to see who held and waited on what. EF Core's EnableRetryOnFailure handles transient retries.",
            "code": {
              "lang": "sql",
              "snippet": "-- Deadlock scenario:\n-- Session A: UPDATE Orders SET...  then UPDATE Customers SET...\n-- Session B: UPDATE Customers SET... then UPDATE Orders SET...\n-- Fix: both sessions access tables in the SAME order\n\n-- Capture deadlocks with Extended Events (query the ring buffer)\nSELECT\n    xdr.value('@timestamp', 'datetime2') AS DeadlockTime,\n    xdr.query('.') AS DeadlockGraph\nFROM (\n    SELECT CAST(target_data AS XML) AS TargetData\n    FROM sys.dm_xe_session_targets t\n    JOIN sys.dm_xe_sessions s ON t.event_session_address = s.address\n    WHERE s.name = 'system_health' AND t.target_name = 'ring_buffer'\n) AS Data\nCROSS APPLY TargetData.nodes('//RingBufferTarget/event[@name=\"xml_deadlock_report\"]') AS XEventData(xdr)\nORDER BY DeadlockTime DESC;\n\n-- Application retry for error 1205\n-- (C# pseudocode: catch SqlException ex when ex.Number == 1205 => retry with backoff)",
              "note": "system_health Extended Events session captures deadlock graphs by default — no setup required"
            },
            "followUps": [
              "How do you set deadlock priority?",
              "What is lock escalation and how can it cause deadlocks?",
              "How does RCSI remove common deadlocks?"
            ],
            "redFlags": [
              "Thinking NOLOCK solves deadlocks",
              "Not knowing SQL Server resolves deadlocks automatically",
              "Not knowing about error 1205 or retry logic"
            ]
          }
        },
        {
          "id": "sql-transactions-004",
          "q": "Explain database normalization. What are 1NF, 2NF, and 3NF? When would you denormalize?",
          "level": "mid",
          "type": "conceptual",
          "freq": "high",
          "tags": ["normalization", "denormalization", "1nf", "2nf", "3nf", "database design"],
          "answer": {
            "oneLiner": "Normalization removes redundancy: 1NF = atomic values, 2NF = no partial dependency on a composite key, 3NF = no transitive dependency. Denormalize to speed up reads.",
            "keyPoints": [
              "1NF: atomic columns, no repeating groups, has a PK",
              "2NF: 1NF + every non-key column depends on the whole PK",
              "3NF: 2NF + no non-key column depends on another non-key column",
              "Denormalize for read-heavy reporting, warehouses (star schema)"
            ],
            "detail": "Examples: 1NF fixes a comma-separated phone column; 2NF moves ProductName out of OrderItems; 3NF moves DepartmentName out of Employees into Departments.",
            "code": {
              "lang": "sql",
              "snippet": "-- Normalized (3NF)\nCREATE TABLE Departments (DepartmentId INT PRIMARY KEY, DepartmentName NVARCHAR(100));\nCREATE TABLE Employees  (EmployeeId INT PRIMARY KEY, Name NVARCHAR(100), DepartmentId INT REFERENCES Departments(DepartmentId));\n\n-- Denormalized (for a read-heavy reporting table)\nCREATE TABLE EmployeeReportView (\n    EmployeeId     INT,\n    EmployeeName   NVARCHAR(100),\n    DepartmentId   INT,\n    DepartmentName NVARCHAR(100)  -- redundant but avoids join in reports\n);",
              "note": "In practice, most OLTP databases target 3NF; data warehouses use star schemas (1-2NF)"
            },
            "followUps": [
              "What is BCNF?",
              "What is a star schema vs 3NF?",
              "How do indexed views avoid physical denormalization?"
            ],
            "redFlags": [
              "Can't give a concrete 2NF/3NF violation example",
              "Thinking normalization always means better performance",
              "Not knowing when to denormalize"
            ]
          }
        },
        {
          "id": "sql-transactions-005",
          "q": "What is the difference between a primary key and a unique key? What is a foreign key?",
          "level": "junior",
          "type": "comparison",
          "freq": "medium",
          "tags": ["primary key", "unique key", "foreign key", "constraints", "null", "clustered"],
          "answer": {
            "oneLiner": "A PK uniquely identifies a row, is NOT NULL, and is clustered by default; a unique key also enforces uniqueness but allows one NULL and is non-clustered; a foreign key enforces that a child value exists in the parent.",
            "keyPoints": [
              "PK: NOT NULL, one per table, clustered by default",
              "UNIQUE: allows one NULL, many per table, non-clustered by default",
              "FK: child value must exist in parent (or be NULL)",
              "FK columns are NOT auto-indexed — index them yourself"
            ],
            "detail": "Without an index on the child's FK column, deleting a parent row scans the whole child table to check dependents. FKs also support ON DELETE/UPDATE CASCADE, SET NULL, or RESTRICT.",
            "code": {
              "lang": "sql",
              "snippet": "CREATE TABLE Departments (\n    DepartmentId   INT         IDENTITY PRIMARY KEY,  -- clustered by default\n    DepartmentCode CHAR(4)     NOT NULL UNIQUE,        -- unique, allows one NULL\n    DepartmentName NVARCHAR(100) NOT NULL\n);\n\nCREATE TABLE Employees (\n    EmployeeId   INT         IDENTITY PRIMARY KEY,\n    Email        NVARCHAR(256) NOT NULL UNIQUE,         -- unique key\n    DepartmentId INT         NOT NULL\n        REFERENCES Departments(DepartmentId)            -- foreign key\n        ON DELETE RESTRICT\n        ON UPDATE CASCADE\n);\n\n-- Index the FK column to avoid table scans on parent DELETE\nCREATE INDEX IX_Employees_DepartmentId ON Employees (DepartmentId);",
              "note": "Always index FK columns on child tables — SQL Server does NOT do this automatically"
            },
            "followUps": [
              "Can a PK column be NULL?",
              "What happens deleting a parent without CASCADE?",
              "Why index FK columns?"
            ],
            "redFlags": [
              "Saying UNIQUE allows multiple NULLs",
              "Not knowing FK columns aren't auto-indexed",
              "Saying a table can have multiple clustered indexes"
            ]
          }
        },
        {
          "id": "sql-transactions-006",
          "q": "How does a clustered index affect the physical storage of a table?",
          "level": "mid",
          "type": "conceptual",
          "freq": "medium",
          "tags": ["clustered index", "physical order", "b-tree", "page splits", "heap", "identity"],
          "answer": {
            "oneLiner": "A clustered index physically orders the rows by its key — the B-tree leaf IS the data — so range scans are fast, but random-key inserts cause page splits and fragmentation.",
            "keyPoints": [
              "Leaf level holds the actual data rows in key order",
              "No clustered index = heap (unordered, RID locators)",
              "All non-clustered indexes carry the clustered key as locator",
              "Random keys (GUIDs) cause page splits; IDENTITY appends sequentially"
            ],
            "detail": "A wide clustered key bloats every non-clustered index. Fix fragmentation with REBUILD (>30%) or REORGANIZE (5-30%); both can update statistics.",
            "code": {
              "lang": "sql",
              "snippet": "-- Check fragmentation\nSELECT\n    i.name AS IndexName,\n    ips.avg_fragmentation_in_percent,\n    ips.page_count\nFROM sys.dm_db_index_physical_stats(DB_ID(), OBJECT_ID('Orders'), NULL, NULL, 'LIMITED') ips\nJOIN sys.indexes i ON ips.object_id = i.object_id AND ips.index_id = i.index_id\nWHERE ips.avg_fragmentation_in_percent > 5;\n\n-- Rebuild (offline, resets fill factor)\nALTER INDEX ALL ON Orders REBUILD WITH (ONLINE = ON);\n\n-- Reorganize (online, minimal locking)\nALTER INDEX IX_Orders_CustomerId ON Orders REORGANIZE;",
              "note": "Rebuild when fragmentation > 30%, reorganize when 5-30%; both update statistics"
            },
            "followUps": [
              "Why are GUIDs bad clustered keys on high-insert tables?",
              "REBUILD vs REORGANIZE?",
              "How does fill factor affect page splits?"
            ],
            "redFlags": [
              "Thinking the clustered index is separate from the table data",
              "Not knowing non-clustered indexes carry the clustered key",
              "Not knowing about page splits from random keys"
            ]
          }
        },
        {
          "id": "sql-transactions-007",
          "q": "Explain key database design decisions: when to use surrogate vs natural keys, and how to design for scalability.",
          "level": "senior",
          "type": "design",
          "freq": "medium",
          "tags": ["database design", "surrogate key", "natural key", "scalability", "sharding", "partitioning"],
          "answer": {
            "oneLiner": "Prefer surrogate keys (INT/BIGINT IDENTITY) for stability and simple joins; use natural keys only when truly unique and immutable. For scale, plan partitioning, archiving, and sharding upfront.",
            "keyPoints": [
              "Surrogate: no business meaning, narrow, stable, sequential",
              "Natural keys risk changing, being NULL, or not unique — enforce with a UNIQUE constraint instead",
              "Partition large tables by date for partition elimination",
              "Add CreatedAt/ModifiedAt from day one; shard by tenant for multi-tenant"
            ],
            "detail": "Keep the clustered key narrow since every non-clustered index carries it. Partition switching lets you archive old partitions in milliseconds.",
            "code": {
              "lang": "sql",
              "snippet": "-- Surrogate PK + separate unique constraint for natural key\nCREATE TABLE Products (\n    ProductId  INT         IDENTITY PRIMARY KEY,   -- surrogate\n    SKU        NVARCHAR(50) NOT NULL UNIQUE,        -- natural key enforced separately\n    Name       NVARCHAR(200) NOT NULL,\n    CreatedAt  DATETIME2    NOT NULL DEFAULT SYSUTCDATETIME()\n);\n\n-- Partition function and scheme for date-based partitioning\nCREATE PARTITION FUNCTION pf_OrderDate (DATE)\n    AS RANGE RIGHT FOR VALUES ('2022-01-01','2023-01-01','2024-01-01','2025-01-01');\n\nCREATE PARTITION SCHEME ps_OrderDate\n    AS PARTITION pf_OrderDate ALL TO ([PRIMARY]);\n\nCREATE TABLE OrdersPartitioned (\n    OrderId   BIGINT IDENTITY,\n    OrderDate DATE NOT NULL,\n    Total     DECIMAL(18,2)\n) ON ps_OrderDate(OrderDate);",
              "note": "Always add CreatedAt/ModifiedAt columns even if not immediately needed — retrofitting is expensive"
            },
            "followUps": [
              "What is partition elimination?",
              "How would you design a multi-tenant Azure SQL DB?",
              "Horizontal sharding vs vertical scaling trade-offs?"
            ],
            "redFlags": [
              "Using SSN or email as a clustered key",
              "Not enforcing natural key uniqueness with surrogate PKs",
              "Ignoring partitioning/archiving for tables over 100M rows"
            ]
          }
        },
        {
          "id": "sql-transactions-008",
          "q": "What is the difference between a primary key and a unique key in terms of NULL handling and index type?",
          "level": "junior",
          "type": "comparison",
          "freq": "medium",
          "tags": ["primary key", "unique key", "null", "clustered", "non-clustered"],
          "answer": {
            "oneLiner": "A PK is NOT NULL and clustered by default (one per table); a unique key allows one NULL and is non-clustered by default (many per table).",
            "keyPoints": [
              "PK: NOT NULL, one per table, clustered by default",
              "UNIQUE: one NULL allowed, many per table, non-clustered by default",
              "Both can be composite",
              "Unique index and unique constraint are functionally identical"
            ],
            "code": {
              "lang": "sql",
              "snippet": "CREATE TABLE Users (\n    UserId    INT          IDENTITY CONSTRAINT PK_Users PRIMARY KEY CLUSTERED,\n    Email     NVARCHAR(256) NOT NULL CONSTRAINT UQ_Users_Email UNIQUE NONCLUSTERED,\n    Username  NVARCHAR(100) NOT NULL CONSTRAINT UQ_Users_Username UNIQUE\n);\n-- PK: clustered, not null\n-- UNIQUE on Email: non-clustered, one NULL allowed (Email is NOT NULL here anyway)",
              "note": "Name your constraints explicitly for easier maintenance and error messages"
            },
            "followUps": [
              "Can an FK reference a UNIQUE column instead of the PK?",
              "How many unique indexes can a table have?",
              "What happens inserting two NULLs into a unique column?"
            ],
            "redFlags": [
              "Saying UNIQUE allows multiple NULLs",
              "Saying a table can have multiple clustered indexes",
              "Not knowing PK is NOT NULL by definition"
            ]
          }
        }
      ]
    }
  ]
}
